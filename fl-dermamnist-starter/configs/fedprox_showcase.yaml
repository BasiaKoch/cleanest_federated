# FedProx "best case" comparison — FedProx side (same setup as fedavg_showcase
# but with the proximal term activated).
client_objective:
  proximal_mu: 0.01
federation:
  local_epochs: 20
  fraction_fit: 0.5
partition:
  strategy: dirichlet
  alpha: 0.1
