% ==============================================================================
%                       *** WORK-IN-PROGRESS STATUS BLOCK ***
%
% Last update: late May 2026, after the Ray-GCS patch was applied to the
% three Flower SLURM templates. This block is a checkpoint so you can
% reopen the file and remember exactly where things stand without
% re-reading the entire conversation history.
%
% --- Jobs currently submitted on Cambridge HPC (re-launched after RAY_TMPDIR
%     patch) ---
%
%   (A) Heterogeneity-ladder Stage A pilot ............ 13 SLURM jobs
%       Script:  mnist_dermnist/scripts/submit_ladder_pilot.sh
%       Output:  mnist_dermnist/results/heterogeneity_ladder/L<level>_<partition>/
%       Tests:   when does FedProx start outperforming FedAvg as
%                heterogeneity worsens? Is the cause label skew, quantity
%                skew, both, or unequal local work?
%       Levels (one seed = 42, FedAvg + FedProx + (FedNova on L1/L3/L4)):
%         L0  two_client_50_50_stratified_iid       IID control
%         L1  two_client_86_14_quantity_only_strat. quantity skew only
%         L2  two_client_50_50_label_skew_only      label skew only
%         L3  two_client_70_30_rare_enriched        moderate mixed skew
%         L4  two_client_90_10_rare_stress          existing severe stress
%       Analysis script (already stubbed, idle until data arrives):
%         results/thesis_ready/scripts/analyse_heterogeneity_ladder.py
%
%   (B) Small-hospital baselines (resubmission of the GCS-failed batch) .. 8 jobs
%       Script:  mnist_dermnist/scripts/submit_small_hospital_baselines.sh
%       Output:
%         two_client_90_10_rare_stress/best_state_*.pt   (checkpoints)
%         small_hospital_local_only/                     (local-only JSONs)
%         small_hospital_finetune/                       (FT JSONs)
%       Tests:
%         1. Federation-value contrast: what does each client gain by
%            joining the federation? (local-only already complete; arrows
%            in Pareto figure already drawn.)
%         2. Personalisation gap: does 5 epochs of local fine-tuning after
%            federation close any remaining gap? Difference between
%            FedAvg+FT and FedAvg, and between FedProx+FT and FedProx,
%            per client. The differential is the novel claim.
%
% --- What is ALREADY in this file (no rework needed) ---
%
%   * Methods chapter: dataset, model, FL setup, algorithms, protocol,
%     implementation, paired-seed protocol, reporting style.
%   * Centralised baseline (mean macro-F1 0.560 +/- 0.024 over 10 seeds).
%   * IID federated baseline (mechanism null).
%   * Engineered non-IID primary result (pure-PyTorch headline +
%     Flower cross-runtime sensitivity).
%   * Mu-sensitivity sweep (5 mu values on engineered partition, single seed).
%   * Per-class breakdown on the engineered partition.
%   * Partition robustness across 4 partition designs.
%   * Parameter-level evidence: per-client update norms.
%   * Computational-work heterogeneity (S0/S1/S2) on engineered partition
%     and the IID isolation contrast.
%   * Asymmetric Li et al. (2020) §5.2 protocol replication.
%   * Partial client participation (C = 0.5) sensitivity.
%   * Small-hospital case study (single seed): all five regimes
%     (Client 0 alone, Client 1 alone, FedAvg, FedProx, centralised)
%     + Pareto figure + rare-class confusion figure.
%
% --- What to add when the HPC jobs finish ---
%
%   1. Heterogeneity-ladder results section (currently missing entirely).
%      Will be a new \section{} probably after partition robustness, with
%      five-row table + delta plot + interpretation paragraph following
%      the eight cases enumerated in the experimental plan.
%
%   2. Two new rows in tab:small-hospital-fed-value
%      (FedAvg+FT/Client X and FedProx+FT/Client X) + a "personalisation
%      gap" paragraph in sec:small-hospital-federation-value.
%
%   3. Two new points on the Pareto scatter (the four fine-tuning regimes
%      should land somewhere between the federated points and the
%      centralised point if local FT successfully personalises).
%
%   4. (OPTIONAL) Volatility-flip analysis. Discovered while looking at
%      the per-client specialty curves: FedProx's Client-0 specialty F1
%      trends DOWN from 0.619 at round 28 to ~0.443 by round 150
%      (smoothed), while FedAvg's is stable. The accompanying volatility
%      profile inverts between algorithms: FedAvg has low common-class
%      volatility (0.033) and high rare-class volatility (0.207); FedProx
%      is the reverse (0.153 / 0.044). Interpretation: proximal anchor
%      gradually reallocates limited model capacity from common classes
%      to rare classes; best-val checkpointing catches FedProx at a
%      "lucky" peak round (146) that test numbers do not reveal as
%      atypical. Would make a strong sub-section if confirmed at 3 seeds;
%      currently single-seed observation only.
%
% --- Known open questions ---
%
%   * Does the ladder show FedProx advantage emerging only at L2+
%     (label skew) and growing through L3/L4, or does it already appear
%     at L1 (quantity only)? Stage A pilot will answer.
%   * Does FedNova help only when local-work counts are unequal? Stage A
%     pilot includes FedNova at L1, L3, L4. If FedNova == FedAvg here,
%     the objective-inconsistency story has empirical support.
%   * Is the Client-0 downward drift under FedProx real or seed-specific?
%     Cannot resolve without multi-seed reruns of the small-hospital
%     partition (which would be the next compute ask).
%   * SCAFFOLD: deferred. Documented as future work; not in the pilot.
%
% --- File / data locations (HPC) ---
%
%   Code:    /home/bk489/federated_clean/cleanest_federated
%   Results: /home/bk489/federated_clean/cleanest_federated/mnist_dermnist/results/
%   Logs:    /home/bk489/federated_clean/cleanest_federated/mnist_dermnist/logs/
%
% When jobs finish on HPC, rsync the relevant result directories down to
% the local repo, then run analyse_heterogeneity_ladder.py and re-open this
% file. The TODO markers below ("% TODO when jobs finish:") point at the
% specific spots that need new content.
% ==============================================================================


% ==============================================================================
% clean_federated_overleaf.tex
%
% Clean, compilable thesis starter file. Currently contains only the
% Centralised Baseline section. Build up the thesis by adding additional
% \section{...} blocks within the Results chapter as each experiment is
% written.
%
% Reporting style: primarily descriptive (mean +/- SD across seeds,
% within-pair Delta, paired-seed wins). Bolding is reserved for rows
% with large within-pair differences relative to across-seed SDs.
% A paired Wilcoxon test and CI are reported for the headline
% engineered comparison; other small effects remain descriptive.
%
% To add a new section in the future:
%   1. Insert \section{Your section title} after the existing one.
%   2. Add \subsection{...} blocks for setup, results, role, summary.
%   3. Place tables inline as in this template, or split into a tables/
%      subfolder and use \input{tables/T01_xxx}.
%   4. Cross-references will resolve once corresponding \label{} markers
%      are added in other sections.
%
% Compiles standalone with pdflatex; no external .bib required yet.
% Bibliography will be added later when references are finalised.
% ==============================================================================

\documentclass[11pt,a4paper]{report}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{graphicx}
\graphicspath{{../figures/}{figures/}{./}}
\usepackage{booktabs}
\usepackage{amsmath, amssymb}
\usepackage{bm}
\usepackage{microtype}
\usepackage{hyperref}
\usepackage{caption}
\usepackage{xcolor}
\usepackage[margin=2.5cm]{geometry}
\usepackage{setspace}
\usepackage[round,authoryear]{natbib}
\onehalfspacing

\title{Federated Optimisation on DermaMNIST:\\
A Paired-Seed Empirical Comparison of FedAvg and FedProx\\
Under Statistical and Computational-Work Heterogeneity}
\author{Barbara Koch}
\date{}

\begin{document}

\maketitle

\tableofcontents
\newpage

% ==============================================================================
\chapter{Methods}
\label{ch:methods}
% ==============================================================================

This chapter describes the dataset, model, federated-learning
setup, algorithms, evaluation protocol, and implementation used
throughout the dissertation. The descriptions are at the
protocol-and-algorithm level; precise hyperparameter values are
collected in summary tables. The training/evaluation pipeline is
held identical across all experiments unless explicitly noted in a
given results section.

% ==============================================================================
\section{Dataset}
\label{sec:methods-dataset}
% ==============================================================================

All experiments use DermaMNIST, the dermatoscopic-imaging
benchmark from the MedMNIST~v2 collection. DermaMNIST is a
seven-class classification task on dermatoscopic skin-lesion
images sampled from HAM10000; representative samples from each
class are shown in Figure~\ref{fig:dataset-classes}. The
benchmark provides a canonical training/validation/test split,
summarised in Table~\ref{tab:dataset-splits}.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_dataset_classes.pdf}
\caption{Representative training sample from each of the seven
DermaMNIST classes.}
\label{fig:dataset-classes}
\end{figure}

\begin{table}[!htbp]
\centering
\caption{DermaMNIST split sizes used throughout the dissertation.}
\label{tab:dataset-splits}
\small
\begin{tabular}{lc}
\toprule
Split & Samples \\
\midrule
Training   & $7{,}007$ \\
Validation & $1{,}003$ \\
Test       & $2{,}005$ \\
\midrule
Total      & $10{,}015$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Class distribution.}
The seven DermaMNIST classes are listed in
Table~\ref{tab:dataset-classes} together with their training-set
prevalence. The distribution is heavily skewed: the majority class
\emph{melanocytic nevi} accounts for $67.05\%$ of the training
set, while the rarest class \emph{dermatofibroma} accounts for
only $1.15\%$. This class imbalance is a feature of clinical
dermatology benchmarks (it reflects real-world incidence rates)
and is preserved without resampling so that all reported metrics
reflect performance on a realistic prior.

\begin{table}[!htbp]
\centering
\caption{DermaMNIST classes and training-set prevalence.}
\label{tab:dataset-classes}
\small
\begin{tabular}{lcc}
\toprule
Class & Training samples & Prevalence \\
\midrule
Actinic keratoses          & $228$   & $3.27\%$  \\
Basal cell carcinoma       & $359$   & $5.13\%$  \\
Benign keratosis           & $769$   & $10.97\%$ \\
Dermatofibroma             & $80$    & $1.15\%$  \\
Melanoma                   & $779$   & $11.11\%$ \\
Melanocytic nevi           & $4{,}693$ & $67.05\%$ \\
Vascular lesions           & $99$    & $1.41\%$  \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Preprocessing.}
DermaMNIST is loaded from the canonical \texttt{dermamnist\_64.npz}
distribution at $64\times64$ RGB resolution and resized to
$28\times28$ for all experiments. The downsampling is a
compute-economy choice: at $28\times28$ each communication round
completes in $\sim 3$--$5$~seconds on an A100, making ten paired
seeds tractable on a single-GPU SLURM allocation. The $28\times28$
resolution is the historical MedMNIST~v1 default and matches the
input resolution at which prior FL-on-DermaMNIST results in the
literature are reported. Pixel values are normalised using the
canonical ImageNet mean and standard deviation.

The $28\times28$ resolution heavily compresses dermatology-relevant
visual features such as pigment networks and asymmetry boundaries,
so absolute test performance reported in this dissertation should
be read as benchmark performance for FL-algorithm comparison rather
than as a clinical-grade dermatology estimate.

% ==============================================================================
\section{Model Architecture}
\label{sec:methods-model}
% ==============================================================================

The classifier used throughout this dissertation is
\texttt{DermMNISTCNN}, a four-block convolutional neural network
with $423{,}175$ trainable parameters. Each block consists of one
$3\times3$ convolution (\texttt{padding=1}) followed by a
GroupNorm normalisation layer, a ReLU activation, and a
$2\times2$ max-pooling operation; the final block replaces
max-pooling with adaptive global average pooling. The four
convolutional blocks expand the feature dimension as
$3 \to 32 \to 64 \to 128 \to 256$; a small fully-connected head
($256 \to 128 \to 7$) with dropout $0.2$ produces the class
logits. Table~\ref{tab:model-summary} summarises the architecture.

\begin{table}[!htbp]
\centering
\caption{\texttt{DermMNISTCNN} architecture summary. Each
convolutional block applies a $3\times3$ convolution (padding
$1$, stride $1$), GroupNorm with group count $g$, ReLU, and
spatial pooling. Input: $3 \times 28 \times 28$ RGB image;
output: $7$-class logits.}
\label{tab:model-summary}
\small
\begin{tabular}{llc}
\toprule
Stage & Operations & Output \\
\midrule
Block 1 & Conv $3 \to 32$ $+$ GN $g{=}4$ $+$ ReLU $+$ MaxPool $2\times2$    & $32 \times 14 \times 14$ \\
Block 2 & Conv $32 \to 64$ $+$ GN $g{=}8$ $+$ ReLU $+$ MaxPool $2\times2$   & $64 \times 7 \times 7$ \\
Block 3 & Conv $64 \to 128$ $+$ GN $g{=}16$ $+$ ReLU $+$ MaxPool $2\times2$ & $128 \times 3 \times 3$ \\
Block 4 & Conv $128 \to 256$ $+$ GN $g{=}16$ $+$ ReLU $+$ AdaptiveAvgPool   & $256$ \\
Head    & Linear $256 \to 128$ $+$ ReLU $+$ Dropout $0.2$ $+$ Linear $128 \to 7$ & $7$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{GroupNorm rather than BatchNorm.}
The choice of GroupNorm rather than BatchNorm is deliberate.
Per-client BatchNorm statistics encode that client's local data
distribution; when the global model is constructed by parameter
averaging, these batch statistics are averaged across heterogeneous
clients in a way that can corrupt the running estimates and harm
generalisation. Hsieh et al.\ (2020) document this effect and
recommend normalisation layers whose statistics are computed
within each example (GroupNorm, LayerNorm) for federated learning
on non-IID data. We adopt GroupNorm following this recommendation.

% ==============================================================================
\section{Federated Learning Setup}
\label{sec:methods-fl-setup}
% ==============================================================================

The federated learning protocol is cross-silo and synchronous,
matching the canonical formulation surveyed in Kairouz et al.\
(2021). The training set is partitioned across $K = 7$ simulated
clients (the per-experiment partition assignment is described
in the relevant Results section). At each communication round
$t$, the central server broadcasts the current global model
parameters $w^t$ to the participating client set
$\mathcal{S}_t \subseteq \{1, \dots, K\}$; each participating
client $i \in \mathcal{S}_t$ performs $E$ epochs of local SGD on
its local training subset $\mathcal{D}_i$, yielding updated
parameters $w_i^{t+1}$. The server then forms the next global
iterate by size-weighted parameter averaging over the
participating set:
\[
w^{t+1} = \sum_{i \in \mathcal{S}_t}
\frac{n_i}{\sum_{j \in \mathcal{S}_t} n_j}\, w_i^{t+1},
\]
where $n_i = |\mathcal{D}_i|$ is the number of training samples
held by client $i$. In the default full-participation
experiments (Sections through partial-participation),
$\mathcal{S}_t = \{1, \dots, K\}$ in every round; the
partial-participation experiment of
Section~\ref{sec:sh-partial-participation} samples $\mathcal{S}_t$
uniformly at random with $|\mathcal{S}_t| = \lfloor C K \rfloor$
clients per round. The size-weighting rule is the standard
FedAvg aggregation of McMahan et al.\ (2017).
Table~\ref{tab:fl-setup} collects the federated-learning
hyperparameters.

\begin{table}[!htbp]
\centering
\caption{Federated learning hyperparameters held constant across
experiments unless explicitly noted in the relevant Results
section.}
\label{tab:fl-setup}
\small
\begin{tabular}{lc}
\toprule
Hyperparameter & Value \\
\midrule
Number of clients $K$                & $7$ \\
Client participation $C$             & $1.0$ (all clients per round) \\
Communication rounds $R$             & $150$ \\
Local epochs $E$                     & $20$ \\
Aggregation rule                     & Size-weighted parameter averaging \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Client count.}
The choice of $K = 7$ clients reflects a deliberate trade-off
between simulating multi-institutional federation and keeping
each client's training set large enough to support stable local
optimisation. With $K = 7$ and $7{,}007$ training samples, each
client receives on the order of $10^3$ samples, which is
sufficient for the 423K-parameter \texttt{DermMNISTCNN} to learn
stably; with $K \gg 10$ the per-client sample count would fall
below the regime in which an architecture of this capacity
converges reliably at $28\times28$ resolution.

% ==============================================================================
\section{Algorithms}
\label{sec:methods-algorithms}
% ==============================================================================

Two federated optimisation algorithms are compared throughout
the dissertation: FedAvg (McMahan et al., 2017) and FedProx
(Li et al., 2020). Both use the same server-side size-weighted
parameter averaging defined above; they differ only in the local
training objective each client minimises.

\paragraph{FedAvg.}
Each client $i$ minimises the standard local cross-entropy loss
on its local data:
\[
\min_{w} \; F_i(w) \;=\; \frac{1}{n_i} \sum_{(x, y) \in \mathcal{D}_i}
\ell\big(f_w(x),\, y\big),
\]
where $\ell$ is the cross-entropy loss and $f_w$ is the network
parameterised by $w$. Local optimisation uses SGD for $E$ epochs
starting from the broadcast global parameters $w^t$.

\paragraph{FedProx.}
Each client $i$ minimises an augmented local objective that adds
a proximal regulariser pulling the local parameters toward the
broadcast global anchor:
\[
\min_{w} \; F_i(w) \;+\; \frac{\mu}{2} \lVert w - w^t \rVert_2^2,
\]
where $\mu \geq 0$ is the proximal coefficient. The first term is
the local objective minimised by FedAvg; the second is the
proximal regulariser. Setting $\mu = 0$ recovers the FedAvg local
objective exactly, so the two algorithms differ only through the
proximal term when $\mu > 0$. Throughout the main experiments we
set $\mu = 0.01$, the value reported by Li et al.\ (2020) as a
robust default across their benchmark matrix; the choice is
fixed a priori and is not tuned per experiment. A dedicated
sensitivity sweep over $\mu \in \{0, 0.001, 0.01, 0.1, 1.0\}$ on
the engineered partition (Section~\ref{sec:mu-sensitivity})
confirms that $\mu = 0.01$ sits at the macro-F1 plateau peak on
this task, so the same value is used in every FedProx run
reported in this dissertation.

\paragraph{Equivalence check at $\mu = 0$.}
The FedProx implementation contains an explicit zero-mu branch
that bypasses the proximal gradient contribution when $\mu = 0$,
making the FedProx local-training loop bit-equivalent to the
FedAvg loop in that regime. This equivalence is verified by
running both algorithms with $\mu = 0$ on the same seed and
confirming identical parameter trajectories. The two algorithms
therefore differ \emph{only} through the $\mu > 0$ proximal
contribution.

% ==============================================================================
\section{Training and Evaluation Protocol}
\label{sec:methods-protocol}
% ==============================================================================

\paragraph{Optimiser and local training.}
All clients use stochastic gradient descent with the
hyperparameters in Table~\ref{tab:training-hparams}. The
local-training loop is identical across both algorithms: $E$
epochs of mini-batched SGD on the client's local training
subset, with the local loss function differing only through the
optional proximal term (Section~\ref{sec:methods-algorithms}).

\begin{table}[!htbp]
\centering
\caption{Optimiser and local-training hyperparameters.}
\label{tab:training-hparams}
\small
\begin{tabular}{lc}
\toprule
Hyperparameter & Value \\
\midrule
Optimiser                        & SGD \\
Learning rate                    & $0.01$ \\
Momentum                         & $0.9$ \\
Weight decay                     & $0$ \\
Batch size                       & $32$ \\
Local epochs per round $E$       & $20$ \\
Local loss                       & Cross-entropy \\
FedProx proximal coefficient $\mu$ & $0.01$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Evaluation.}
The server evaluates the global model on the centralised
validation set after every communication round, recording
validation cross-entropy loss, validation accuracy, validation
balanced accuracy, validation macro-F1, and per-class
validation F1. At the end of training, the test set is
evaluated \emph{once} on the global model produced at the
round of highest validation macro-F1 (the
\emph{best-validation checkpoint} protocol). Test-set
macro-F1 at this checkpoint is the headline metric reported
throughout the dissertation; per-class F1, balanced accuracy,
and overall accuracy on the test set are reported as
secondary measures. The validation set is held fixed across
all experiments; the test set is never seen by the model
during training and is evaluated exactly once per run.

% ==============================================================================
\section{Paired-Seed Protocol}
\label{sec:methods-paired-seed}
% ==============================================================================

A defining design choice of the experimental protocol used in
this dissertation is the \emph{paired-seed} comparison
structure. For every FedAvg-vs-FedProx contrast reported in
the Results chapter, the two algorithms are run on the same
set of ten random seeds: $\{42,\, 123,\, 456,\, 789,\, 999,\,
2024,\, 31337,\, 8675309,\, 161803,\, 271828\}$. Within each
seed, the following sources of stochasticity are fixed to
identical values across the FedAvg and FedProx runs:

\begin{itemize}
\setlength\itemsep{2pt}
\item the random partition of training samples to clients;
\item the initial model parameters $w^0$;
\item the per-(round, client) dataloader shuffle order;
\item the per-(round, client) local-epoch schedule, where
      applicable (for the system-heterogeneity experiments
      in Chapter~\ref{ch:results});
\item the random sampling of participating clients per round,
      where applicable.
\end{itemize}

Because all these sources of stochasticity are matched
within-pair, the within-pair difference
\[
\Delta_s \;=\; \text{test macro-F1}_{\text{FedProx}, s}
              - \text{test macro-F1}_{\text{FedAvg}, s}
\]
on seed $s$ reflects only the algorithmic difference (the
gated proximal term), not random-state divergence. Aggregate
within-pair differences across the ten seeds therefore have
substantially tighter variance than the corresponding
independent-seed comparison would, allowing meaningful
estimates of small algorithmic effects at modest sample size.

\paragraph{Reporting style.}
We follow the convention of the federated-learning literature
(McMahan et al.\ 2017, Li et al.\ 2020, Wang et al.\ 2020,
Hsu et al.\ 2019, Acar et al.\ 2021) in reporting paired
results primarily using mean $\pm$ standard deviation across
seeds, visual curve comparisons with shaded $\pm$~SEM bands,
within-pair $\Delta$ values, and the count of paired-seed
wins. Bolding in tables is reserved for rows where the
within-pair difference is large relative to the across-seed
SDs; rows whose differences are within the across-seed SDs
are reported descriptively without bolding. For the headline
engineered comparison, we additionally report a paired
Wilcoxon signed-rank test and a confidence interval on the
within-pair difference to quantify the strength of the primary
result and its cross-runtime replication. Other small partition
and computational-work heterogeneity effects are interpreted
descriptively unless explicitly stated otherwise; differences
smaller in magnitude than the across-seed standard deviation
are described as ``descriptively positive/negative'' rather
than as significant or null effects.

% ==============================================================================
\section{Implementation and Compute}
\label{sec:methods-implementation}
% ==============================================================================

The experiments are implemented in two complementary code paths
that share the same algorithmic logic but use different runtimes.

\paragraph{Pure-PyTorch reference loop.}
A single-process sequential implementation in PyTorch that
iterates through clients in deterministic order, holds the
global parameter state in-memory, and performs explicit
size-weighted aggregation after each round. This implementation
is used as the algorithmic reference and is the primary runtime
for the headline statistical-heterogeneity result
(Section~\ref{sec:engineered-headline}).

\paragraph{Flower simulation runtime.}
A federated-learning simulation using the Flower framework
(Beutel et al., 2020) with each client implemented as a Ray
actor. The server-side aggregation passes through Flower's
\texttt{FedAvg} strategy with size-weighted averaging; the
client-side FedAvg/FedProx local training loop is functionally
identical to the pure-PyTorch path. This runtime is used for
the cross-runtime sensitivity check
(Section~\ref{sec:runtime-sensitivity}) and for all
robustness, system-heterogeneity, and partial-participation
experiments in the Results chapter.

The two implementations have been verified to use bit-equivalent
local objectives at $\mu = 0$ (the FedProx code path collapses
to FedAvg in the $\mu = 0$ branch in both runtimes). Differences
in per-seed results between the two runtimes therefore arise
from implementation-level factors --- floating-point
non-associativity of summation order in aggregation, parameter
serialisation across Ray actor process boundaries, and
random-number-generator routing through Ray scheduling ---
rather than from algorithmic differences. The cross-runtime
sensitivity check in Section~\ref{sec:runtime-sensitivity}
characterises the magnitude of this implementation-level
variance.

\paragraph{Compute.}
Pure-PyTorch experiments are executed on the Cambridge HPC
ampere partition (NVIDIA A100~80GB). Flower-runtime
experiments are executed on either the same Cambridge HPC
partition or on rented A100/H100 instances on RunPod where
queue priority constrains turnaround. All result JSON files
record the host machine, CUDA device, framework version, git
commit, and start/finish timestamps for full reproducibility.
A single training run completes in approximately $5$~minutes
(centralised) to $20$~minutes (federated $150$-round) on an
A100, with FedProx runs taking $\sim 40\%$ longer than FedAvg
runs at matched conditions due to the per-step computation of
the proximal regulariser.

\paragraph{Reproducibility.}
The complete experimental matrix can be reproduced from the
SLURM submission scripts in \texttt{mnist\_dermnist/scripts/}
in the project repository. Each script declares the partition,
seed list, algorithm, and hyperparameters explicitly; together
with the run-time provenance recorded in each result JSON,
this makes every reported number traceable to a specific
submission script, git commit, and machine.

% ==============================================================================
\chapter{Results and Discussion}
\label{ch:results}
% ==============================================================================

This chapter reports the empirical findings of the dissertation,
starting with a centralised non-federated reference and then
progressing through the federated experiments. Methodological
observations and mechanism interpretation are interleaved with the
empirical results rather than relegated to a separate discussion
chapter.

% ==============================================================================
\section{Centralised Baseline}
\label{sec:centralised-baseline}
% ==============================================================================

Before evaluating any federated configuration, we establish a
centralised non-federated reference. The centralised baseline trains
the same \texttt{DermMNISTCNN} architecture used in the federated
experiments on the pooled DermaMNIST training set with no
federation. It provides a pooled-training reference under the
same architecture, preprocessing, and comparable optimisation
settings, against which the federated results reported in the
remainder of this chapter are interpreted as fractional
recoveries of the centralised performance.

\subsection{Training setup}
\label{sec:centralised-setup}

The centralised baseline pools all $7{,}007$ training samples,
$1{,}003$ validation samples, and $2{,}005$ test samples of the
DermaMNIST benchmark into a single non-federated training loop.
The model architecture is held identical to the federated
experiments: a four-block GroupNorm CNN with $423{,}175$ trainable
parameters operating on $28\times28$ RGB inputs. The optimiser is
SGD with learning rate $0.01$, momentum $0.9$, weight decay $0$,
and batch size $32$ --- the same hyperparameters used by each
client in the subsequent federated experiments, chosen for direct
comparability rather than for centralised optimum. The model is
trained for $50$ epochs; test set performance is reported at the
epoch of highest validation macro-F1 (the
\emph{best-validation checkpoint} protocol).

To match the federated experiments and enable paired-seed analyses
where applicable, the centralised baseline is repeated across the
same ten random seeds used for all federated runs:
$\{42,\,123,\,456,\,789,\,999,\,2024,\,31337,\,8675309,\,161803,\,$
$271828\}$. Within each seed the data shuffle, model
initialisation, and training trajectory are deterministic; the
across-seed variation quantifies optimisation noise on this task.

\subsection{Results}
\label{sec:centralised-results}

Table~\ref{tab:centralised-results} reports test performance at
the best-validation checkpoint across the ten seeds. The
centralised model reaches a mean test macro-F1 of
$0.560\pm0.024$, mean classification accuracy of
$0.764\pm0.012$, and mean balanced accuracy of
$0.561\pm0.034$. The per-seed test macro-F1 ranges from
$0.518$ to $0.597$. The best-validation epoch is consistently in
the second half of training (median $47$ out of $50$ epochs),
indicating that the $50$-epoch training budget is appropriate
for this task and architecture.

\begin{table}[!htbp]
\centering
\caption{Centralised baseline test performance at the
best-validation checkpoint, reported as mean $\pm$ standard
deviation across $n=10$ seeds. Per-class F1 is the F1 score on
each of the seven DermaMNIST classes; class prevalence in the
training set is shown in parentheses.}
\label{tab:centralised-results}
\small
\begin{tabular}{lc}
\toprule
Metric & Test value (mean $\pm$ SD) \\
\midrule
Macro-F1                        & $\mathbf{0.5600 \pm 0.0240}$ \\
Balanced accuracy               & $0.5608 \pm 0.0341$ \\
Overall accuracy                & $0.7637 \pm 0.0123$ \\
Cross-entropy loss              & $0.7778 \pm 0.0653$ \\
\midrule
\multicolumn{2}{l}{\textit{Per-class F1:}} \\
\quad Actinic keratoses (3.27\%)              & $0.4923 \pm 0.0433$ \\
\quad Basal cell carcinoma (5.13\%)           & $0.5380 \pm 0.0263$ \\
\quad Benign keratosis (10.97\%)              & $0.5285 \pm 0.0397$ \\
\quad Dermatofibroma (1.15\%)                 & $0.3359 \pm 0.0961$ \\
\quad Melanoma (11.11\%)                      & $0.4551 \pm 0.0349$ \\
\quad Melanocytic nevi (67.05\%)              & $0.8896 \pm 0.0088$ \\
\quad Vascular lesions (1.41\%)               & $0.6807 \pm 0.0680$ \\
\bottomrule
\end{tabular}
\end{table}

The per-class breakdown reveals the task's structure clearly.
The majority class \emph{melanocytic nevi} ($67\%$ prevalence)
is the easiest to learn, reaching $F1 = 0.890$ with very small
across-seed variance ($\pm 0.009$). The two rarest classes,
\emph{dermatofibroma} ($1.15\%$ prevalence, $80$ training
samples) and \emph{vascular lesions} ($1.41\%$ prevalence,
$99$ training samples), show the largest across-seed variance
($\pm 0.096$ and $\pm 0.068$), reflecting the difficulty of
stably learning classes represented by such small absolute
counts. The two clinically critical classes \emph{melanoma}
and \emph{basal cell carcinoma} reach mean F1 of $0.455$ and
$0.538$ respectively --- well above chance but substantially
below the majority class, consistent with the medical-imaging
literature on small CNNs at $28\times28$ resolution.

The macro-F1 of $0.56$ is a benchmark-typical result for
DermaMNIST at this resolution and architecture, comparable to
published values on the MedMNIST v2 benchmark. It is not
intended as a clinical-grade performance estimate; the input
resolution is deliberately compressed and the architecture is a
small CNN, both chosen for comparability across the federated
experiments in this dissertation rather than for clinical
deployment.

The centralised mean of $0.560 \pm 0.024$ macro-F1 is used as the
reference for federated recovery percentages and as the noise floor
($\pm 0.024$) below which seed-level differences are read as
descriptive. The reference is not an upper bound: the architecture,
preprocessing, and optimiser were chosen for comparability with the
federated experiments rather than for maximum centralised
performance.

% ==============================================================================
\section{IID Federated Baseline}
\label{sec:iid-baseline}
% ==============================================================================

Before evaluating the FedProx-vs-FedAvg comparison under
heterogeneous client data distributions, we establish a federated
baseline in which every client holds an i.i.d.\ sample of the
global training distribution. The IID configuration is the
\emph{mechanism null} condition: when local objectives $F_i$ are
i.i.d.\ samples of the same underlying distribution, the bounded-
dissimilarity argument of Li et al.\ (2020)~\S4.1 predicts that
no inter-client drift exists for the proximal anchor to constrain
and FedProx should provide no benefit over FedAvg. A positive
$\Delta$ under IID would therefore contradict the mechanism
interpretation that the subsequent non-IID experiments rely on.
This section confirms the mechanism null empirically.

\subsection{Partition and training setup}
\label{sec:iid-setup}

The IID partition (\texttt{iid\_7\_clients}) assigns the
$7{,}007$ training samples to seven simulated clients by uniform
random shuffling of the global index list and splitting into
seven equal shards. Each client therefore receives the same
number of samples and, in expectation, the same class
composition as the global training set --- so client-local
distributions differ from one another only through finite-sample
sampling noise, not by design.

Figure~\ref{fig:iid-partition} visualises the resulting
per-(client, class) sample counts at seed $42$. All seven
clients hold exactly $1{,}001$ samples, and each client's
stacked composition mirrors the global class distribution within
the small variation expected from random partitioning of a
$7{,}007$-sample pool. In absolute counts, every client receives
approximately $670$ melanocytic nevi, $110$ melanoma, $110$
benign keratosis, $50$ basal cell carcinoma, $33$ actinic
keratoses, $14$ vascular lesions, and $11$ dermatofibroma
samples; all seven classes are present in every client, including
the three rarest ones. The IID partition therefore represents
the limit case in which no client carries class-specialised
information: any class that a client must learn to predict, it
already sees locally in roughly the global proportion.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.95\textwidth]{F_iid_partition.pdf}
\caption{IID partition of the $7{,}007$-sample DermaMNIST
training set across the seven simulated clients (seed $42$).
Each bar is one client; segment heights show the number of
samples of each class. All clients receive exactly $1{,}001$
samples and the stacks are visually near-identical, confirming
that the per-client class composition matches the global
distribution up to finite-sample sampling noise.}
\label{fig:iid-partition}
\end{figure}

All other hyperparameters match the centralised baseline of
Section~\ref{sec:centralised-baseline}: identical
\texttt{DermMNISTCNN} architecture, SGD with learning rate
$0.01$, momentum $0.9$, batch size $32$, weight decay $0$.
Federated-specific hyperparameters are: full participation
($C = 1.0$, all seven clients per round), $R = 150$ communication
rounds, $E = 20$ local epochs, FedProx proximal coefficient
$\mu = 0.01$. The Flower simulation framework is used as the
canonical FL runtime throughout. The same ten paired seeds as
the centralised baseline are used; within each seed the partition,
initial model weights, and client-side data shuffling are
deterministic and identical across the FedAvg and FedProx arms,
so within-pair differences reflect only the algorithmic gating
of the proximal term.

\subsection{Results}
\label{sec:iid-results}

Table~\ref{tab:iid-results} reports test performance at the
best-validation checkpoint across the ten seeds. FedAvg and
FedProx reach indistinguishable mean test macro-F1
($0.585 \pm 0.020$ and $0.579 \pm 0.019$ respectively); the
within-pair mean difference is $-0.006 \pm 0.019$ and FedProx
wins on $5$ of $10$ paired seeds.

\begin{table}[!htbp]
\centering
\caption{IID federated baseline. Test performance at the
best-validation checkpoint, mean $\pm$ standard deviation across
$n = 10$ paired seeds. The winning algorithm per row is shown in
bold; for rows where the two algorithms are within $\pm 0.001$
neither is bolded. Per-class F1 is the F1 score on each of the
seven DermaMNIST classes, with class prevalence in the training
set in parentheses --- these prevalences are global and, under
IID partitioning, also describe each client's expected local
class distribution (see Figure~\ref{fig:iid-partition}).}
\label{tab:iid-results}
\small
\begin{tabular}{lccc}
\toprule
Metric & FedAvg & FedProx & $\Delta$ \\
\midrule
Test macro-F1                 & $\mathbf{0.5846 \pm 0.0197}$ & $0.5786 \pm 0.0190$ & $-0.0061$ \\
Balanced accuracy             & $\mathbf{0.5852 \pm 0.0290}$ & $0.5802 \pm 0.0245$ & $-0.0050$ \\
Overall accuracy              & $0.7625 \pm 0.0094$          & $0.7613 \pm 0.0083$ & $-0.0012$ \\
\midrule
\multicolumn{4}{l}{\textit{Per-class F1:}} \\
\quad Actinic keratoses (3.27\%)   & $\mathbf{0.523 \pm 0.034}$ & $0.507 \pm 0.030$ & $-0.016$ \\
\quad Basal cell carcinoma (5.13\%)& $\mathbf{0.557 \pm 0.011}$ & $0.542 \pm 0.034$ & $-0.015$ \\
\quad Benign keratosis (10.97\%)   & $0.538 \pm 0.024$          & $0.537 \pm 0.020$ & $-0.001$ \\
\quad Dermatofibroma (1.15\%)      & $\mathbf{0.412 \pm 0.075}$ & $0.406 \pm 0.069$ & $-0.006$ \\
\quad Melanoma (11.11\%)           & $\mathbf{0.462 \pm 0.020}$ & $0.456 \pm 0.018$ & $-0.007$ \\
\quad Melanocytic nevi (67.05\%)   & $0.894 \pm 0.004$          & $0.894 \pm 0.004$ & $+0.000$ \\
\quad Vascular lesions (1.41\%)    & $0.707 \pm 0.040$          & $0.708 \pm 0.036$ & $+0.001$ \\
\bottomrule
\end{tabular}
\end{table}

The two algorithms are descriptively indistinguishable across
all reported metrics. The mean within-pair difference is
small and negative ($-0.006$ macro-F1, $-0.005$ balanced
accuracy) but lies within the across-seed standard deviation of
either algorithm. The per-class breakdown shows the same pattern
on every class: differences are at most $0.016$ in absolute
value and have no consistent direction. Notably, the majority
class \emph{melanocytic nevi} produces a per-pair difference
of $+0.0001$ macro-F1 with FedProx and FedAvg matching to three
decimal places ($0.894$ vs $0.894$); this is the most
mechanistically transparent class because it is structurally
held by every client at the same proportion, and its near-zero
$\Delta$ is the expected signature of an inert proximal anchor.

Figure~\ref{fig:iid-convergence} shows the per-round validation
macro-F1 trajectories on the IID partition; the FedAvg and FedProx
curves are essentially superimposed across all 150 rounds,
visually confirming the null $\Delta$ reported in
Table~\ref{tab:iid-results}.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.85\textwidth]{F_iid_convergence.pdf}
\caption{Validation macro-F1 over 150 communication rounds on
the IID partition (Flower runtime, $n = 10$ paired seeds,
mean $\pm$ SEM band). FedAvg and FedProx produce essentially
identical trajectories from round $\sim 10$ onwards, consistent
with the proximal-anchor mechanism's prediction that the
regulariser is inert when no inter-client class skew exists.}
\label{fig:iid-convergence}
\end{figure}

An independent IID replication is available in
\texttt{results/iid/}, which uses the same partition,
hyperparameters, and ten seeds but was run as a separate sweep.
The independent replication produces a mean within-pair
difference of $-0.007 \pm 0.022$ macro-F1 with FedProx winning
$4$ of $10$ paired seeds, agreeing with the canonical IID
baseline to within $0.001$ macro-F1. The two independent sweeps
converging on the same near-zero $\Delta$ provides additional
evidence that the IID null is genuine rather than a feature of
any single sweep's random seed draws.

The IID across-seed SD of $\pm 0.020$ macro-F1 sets the
paired-comparison noise floor for the rest of the chapter. The
near-zero $\Delta$ supports the interpretation that any positive
$\Delta$ under non-IID partitions is not a partition-independent
advantage of FedProx; it does not rule out interactions with
runtime or stochasticity.

% ==============================================================================
\section{Engineered Non-IID Partition: Primary Result}
\label{sec:engineered-headline}
% ==============================================================================

This section reports the primary non-IID result: FedAvg vs.\
FedProx on the engineered \texttt{balanced\_paired\_7\_clients}
partition under two independent runtimes.

\subsection{Partition design}
\label{sec:engineered-partition}

The engineered partition assigns each of the six minority classes
to exactly two of the seven clients (a \emph{paired-co-training}
design), with melanocytic nevi held in equal proportion by all
seven. Pairings: $C_0, C_1$ hold actinic keratoses + basal cell
carcinoma; $C_2, C_3$ hold benign keratosis + dermatofibroma;
$C_4, C_5$ hold melanoma + vascular lesions; $C_6$ is a
nevi-only generalist. Within each pair, samples are split as
evenly as the integer counts allow. Per-client totals range from
$673$ to $1{,}110$ (a $1.65\times$ quantity range), keeping
quantity skew mild relative to label skew so that the partition
isolates the label-skew axis. Each minority class is thus held by
two co-trained clients that drift apart locally and must be
reconciled at aggregation; the majority class is held by every
client and acts as a within-experiment per-class control.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.95\textwidth]{F_engineered_partition.pdf}
\caption{Engineered \texttt{balanced\_paired\_7\_clients}
partition of the $7{,}007$-sample DermaMNIST training set.
Each bar is one client; segment heights show the number of
samples of each class. Per-client totals range from $673$ to
$1{,}110$. Each minority class appears on exactly two clients
(its co-training pair), and four of the seven clients hold no
samples of any given minority class --- the structural label
skew that the FedProx proximal anchor is designed to control.
Direct visual comparison with Figure~\ref{fig:iid-partition}
makes the qualitative difference between the two partitions
explicit.}
\label{fig:engineered-partition}
\end{figure}

\subsection{Training setup}
\label{sec:engineered-setup}

Hyperparameters are identical to the IID baseline; only the
partition differs. The paired comparison is run on both runtimes
defined in Section~\ref{sec:methods-implementation} (pure-PyTorch
reference and Flower simulation), so runtime-induced variation
can be assessed alongside the headline effect.

\subsection{Results}
\label{sec:engineered-results}

Table~\ref{tab:engineered-results} reports test performance at
the best-validation checkpoint across the ten paired seeds on
each runtime. Figure~\ref{fig:engineered-convergence} shows the
per-round validation macro-F1 trajectories.

\begin{table}[!htbp]
\centering
\caption{Engineered \texttt{balanced\_paired\_7\_clients}
partition. Test macro-F1 at the best-validation checkpoint, mean
$\pm$ standard deviation across $n = 10$ paired seeds. The winning
algorithm per runtime is shown in bold. The $\Delta$ column is the
mean within-pair difference (FedProx $-$ FedAvg); $95\%$~CI is the
paired-$t$ confidence interval on the within-pair differences; the
$p$-value is the two-sided paired Wilcoxon signed-rank test on the
per-seed differences.}
\label{tab:engineered-results}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lcccccc}
\toprule
Runtime & FedAvg & FedProx & $\Delta$ & $95\%$ CI ($\Delta$) & Wilcoxon $p$ & Wins \\
\midrule
Pure-PyTorch (reference) & $0.4814 \pm 0.0254$ & $\mathbf{0.5081 \pm 0.0140}$ & $+0.0267$ & $[+0.002,\ +0.052]$ & $0.020$ & $9/10$ \\
Flower (simulation)      & $0.4964 \pm 0.0150$ & $\mathbf{0.5033 \pm 0.0206}$ & $+0.0069$ & $[-0.011,\ +0.024]$ & $0.43$  & $7/10$ \\
\bottomrule
\end{tabular}
\end{table}

FedProx outperforms FedAvg in mean macro-F1 under both runtimes,
with FedProx winning the majority of paired seeds. On the
pure-PyTorch reference the within-pair difference is $+0.027$,
Wilcoxon $p = 0.020$, $95\%$ CI $[+0.002,\ +0.052]$ (CI excludes
zero); on Flower the difference is $+0.007$, $p = 0.43$, $95\%$
CI $[-0.011,\ +0.024]$ (CI includes zero). Direction replicates;
magnitude differs by a factor of approximately four.
Figure~\ref{fig:engineered-convergence} shows FedProx maintaining
a higher validation macro-F1 throughout training under both
runtimes, with a visibly larger gap under pure-PyTorch.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_engineered_convergence.pdf}
\caption{Validation macro-F1 over 150 communication rounds on
the engineered \texttt{balanced\_paired\_7\_clients} partition
under two independent implementations ($n = 10$ paired seeds,
mean $\pm$ SEM band). FedProx (dark slate blue) maintains a
higher trajectory than FedAvg (mint green) under both runtimes;
the gap is visibly larger under pure-PyTorch (left) than under
Flower (right).}
\label{fig:engineered-convergence}
\end{figure}

\subsection{Runtime sensitivity}
\label{sec:runtime-sensitivity}

The per-seed differences $\Delta_{\mathrm{PT},s} -
\Delta_{\mathrm{Flower},s}$ are not consistently signed (5 seeds
each way), so the runtime gap is a noisy mean-level effect rather
than a systematic per-seed shift. Plausible contributors:
floating-point non-associativity in Flower's actor-return-ordered
aggregation, RNG routing through Ray scheduling, and parameter
serialisation across process boundaries; none are algorithmic.
The pure-PyTorch result is therefore treated as the reference-loop
primary result; the Flower result is a runtime-sensitivity
qualification rather than a full replication of the effect size.

% ==============================================================================
\section{Sensitivity to the Proximal Coefficient $\mu$}
\label{sec:mu-sensitivity}
% ==============================================================================

All FedProx experiments in this dissertation use $\mu = 0.01$, the
default reported by Li et al.\ (2020) and fixed a priori
(Section~\ref{sec:methods-algorithms}). Before the deeper
per-class, robustness, parameter-level, and computational-work
heterogeneity analyses that follow, we verify this choice with a
sweep over $\mu$ on the engineered partition just established.
Li et al.\ (2020), §5 perform the analogous sweep on Synthetic,
FEMNIST, Shakespeare, and Sent140.

\subsection{Setup}
\label{sec:mu-sensitivity-setup}

The sweep is run on the same engineered
\texttt{balanced\_paired\_7\_clients} partition as the headline of
Section~\ref{sec:engineered-headline} on the Flower runtime, with
the same ten paired seeds and all other hyperparameters held
identical ($E = 20$, $R = 150$, $C = 1.0$, no computational-work
heterogeneity, identical optimiser settings). Only $\mu$ varies:
$\mu = 0$ (reduces to FedAvg, the baseline reference) and
$\mu \in \{0.001, 0.01, 0.1, 1.0\}$, matching the Li et al.\
(2020) grid. Test macro-F1 is reported at each seed's
best-validation checkpoint and per-client update norms
$\lVert w_i^{t+1} - w^t \rVert_2$ are recorded for the
parameter-level panel.

\subsection{Results}
\label{sec:mu-sensitivity-results}

Table~\ref{tab:mu-sensitivity} reports test macro-F1, the paired
within-pair $\Delta$ against the $\mu = 0$ FedAvg baseline, and the
mean per-client update norm for each value of $\mu$.
Figure~\ref{fig:mu-sensitivity-outcome} pairs the endpoint
(mean test macro-F1 vs.\ $\mu$ with $95\%$ bootstrap CI) with the
validation-macro-F1 trajectory for each $\mu$, and
Figure~\ref{fig:mu-sensitivity-mechanism} shows the parameter-level
counterpart (mean per-client update norm and inter-client
dispersion vs.\ $\mu$).

\begin{table}[!htbp]
\centering
\caption{FedProx proximal-coefficient sweep on the engineered
partition (Flower runtime, $n = 10$ paired seeds). Test macro-F1
is reported as mean $\pm$ standard deviation across seeds; the
paired $\Delta$ is the mean within-pair difference against the
$\mu = 0$ FedAvg baseline run on the same seeds; the update-norm
column is the mean per-client $\lVert w_i^{t+1} - w^t \rVert_2$
averaged over all (round, client) pairs and across seeds. Bolding
marks the row at the macro-F1 plateau peak.}
\label{tab:mu-sensitivity}
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{@{}l c r c c@{}}
\toprule
Setting             & Test macro-F1      & $\Delta$ vs.\ $\mu = 0$ & Wins  & Mean $\lVert \Delta w \rVert$ \\
\midrule
$\mu = 0$ (FedAvg)  & $0.493 \pm 0.020$  & ---                      & ---    & $1.529$ \\
$\mu = 0.001$       & $0.504 \pm 0.021$  & $+0.011$                 & $6/10$ & $1.459$ \\
$\mu = 0.01$        & $\mathbf{0.506 \pm 0.017}$ & $\mathbf{+0.013}$ & $\mathbf{6/10}$ & $\mathbf{1.024}$ \\
$\mu = 0.1$         & $0.479 \pm 0.017$  & $-0.014$                 & $2/10$ & $0.614$ \\
$\mu = 1.0$         & $0.419 \pm 0.013$  & $-0.074$                 & $0/10$ & $0.323$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_mu_sensitivity_outcome_and_convergence.pdf}
\caption{FedProx proximal-coefficient sweep on the engineered
partition (Flower runtime, $n = 10$ paired seeds). \textbf{(a)}
Final test macro-F1 at the best-validation checkpoint as a
function of $\mu$; markers show the mean, bars show the $95\%$
percentile bootstrap CI over $10{,}000$ resamples, and the dashed
green line marks the $\mu = 0$ FedAvg reference. \textbf{(b)}
Validation macro-F1 trajectory over $150$ communication rounds
for each value of $\mu$, mean across seeds with $\pm$SEM bands.
Together the panels show that the endpoint plateau at
$\mu \in \{0.001, 0.01\}$ in (a) corresponds to FedAvg-like
trajectories in (b), while the endpoint collapse at $\mu = 1.0$
reflects a slowly converging trajectory that never recovers.}
\label{fig:mu-sensitivity-outcome}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_mu_sensitivity_mechanism.pdf}
\caption{Parameter-level summary of the proximal-coefficient sweep
on the engineered partition. \textbf{(a)} Mean per-client update
norm $\lVert \Delta w \rVert_2$ as a function of $\mu$.
\textbf{(b)} Mean inter-client dispersion (the round-wise standard
deviation of update norms across clients, then averaged over
rounds) as a function of $\mu$. Both quantities decrease
monotonically as $\mu$ grows.}
\label{fig:mu-sensitivity-mechanism}
\end{figure}

\subsection{Interpretation}
\label{sec:mu-sensitivity-interpretation}

\paragraph{Inverted-U on test macro-F1.}
Test macro-F1 rises from $\mu = 0$ to a plateau at
$\mu \in \{0.001, 0.01\}$, then falls below the FedAvg reference at
$\mu = 0.1$ and collapses at $\mu = 1.0$. The chosen value
$\mu = 0.01$ lies on the plateau peak; the within-pair $\Delta$
against FedAvg is positive only for $\mu \leq 0.01$ and turns
negative beyond that. This is the same qualitative shape Li et al.\
(2020) report for their non-convex tasks: a sweet spot at small but
nonzero $\mu$, with strong regularisation harmful and weak
regularisation indistinguishable from FedAvg. The empirical sweet
spot on this task therefore coincides with the literature default
rather than being recovered by retroactive tuning, and $\mu = 0.01$
is retained as the operating point for all subsequent FedProx
experiments in the dissertation.

\paragraph{Monotonic local-update shrinkage.}
The mean per-client update norm decreases monotonically as $\mu$
grows, from $1.529$ at $\mu = 0$ to $0.323$ at $\mu = 1.0$
(a $4.7\times$ reduction across two orders of magnitude in $\mu$),
and inter-client dispersion of update norms shows the same
monotonic pattern. The proximal regulariser's effect on
local-update magnitude scales smoothly with its coefficient, as
expected from its $L_2$ penalty form. The single-$\mu$ form of
this parameter-level result --- specifically the engineered-vs-IID
partition contrast at $\mu = 0.01$ --- is reported in detail in
Section~\ref{sec:update-norms}. Smaller updates under FedProx are
consistent with drift control but do not uniquely distinguish
drift control from broader local-update regularisation.

% ==============================================================================
\section{Per-Class Breakdown on the Engineered Partition}
\label{sec:engineered-per-class}
% ==============================================================================

The $+0.027$ aggregate from the previous section is decomposed
here by class. The data are the same ten paired pure-PyTorch
runs; per-class F1 is read from each run's
\texttt{per\_class\_f1} field at the best-validation checkpoint.

\begin{table}[!htbp]
\centering
\caption{Per-class test F1 on the engineered partition (pure-PyTorch
primary, $n = 10$ paired seeds, mean $\pm$ standard deviation across
seeds). Bolding is reserved for rows where the within-pair $\Delta$
is large relative to the across-seed SDs (here: melanoma and actinic
keratoses); all other rows are within their across-seed noise and
are reported descriptively. The $\Delta$ column is the mean
within-pair difference (FedProx $-$ FedAvg). Wins is the count of
paired seeds on which FedProx outperforms FedAvg on that class.}
\label{tab:engineered-per-class}
\footnotesize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.15}
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}l c c r c@{}}
\toprule
Class (prevalence)            & FedAvg                     & FedProx                    & $\Delta$          & Wins            \\
\midrule
Actinic keratoses (3.27\%)    & $0.404 \pm 0.111$          & $\mathbf{0.471 \pm 0.040}$ & $\mathbf{+0.067}$ & $\mathbf{9/10}$ \\
Basal cell carcinoma (5.13\%) & $0.475 \pm 0.046$          & $0.473 \pm 0.027$          & $-0.001$          & $4/10$          \\
Benign keratosis (10.97\%)    & $0.440 \pm 0.033$          & $0.444 \pm 0.041$          & $+0.004$          & $7/10$          \\
Dermatofibroma (1.15\%)       & $0.283 \pm 0.141$          & $0.296 \pm 0.070$          & $+0.013$          & $4/10$          \\
Melanoma (11.11\%)            & $0.187 \pm 0.101$          & $\mathbf{0.301 \pm 0.026}$ & $\mathbf{+0.114}$ & $\mathbf{9/10}$ \\
Melanocytic nevi (67.05\%)    & $0.884 \pm 0.003$          & $0.886 \pm 0.004$          & $+0.002$          & $7/10$          \\
Vascular lesions (1.41\%)     & $0.698 \pm 0.078$          & $0.686 \pm 0.043$          & $-0.012$          & $5/10$          \\
\bottomrule
\end{tabular}%
}
\end{table}

The benefit is non-uniform: melanoma drives the headline
($\Delta = +0.114$, $9/10$ wins, across-seed SD drops from
$\pm 0.101$ to $\pm 0.026$), actinic keratoses contributes
secondarily ($+0.067$, $9/10$), and the remaining five classes
show $-0.012 \leq \Delta \leq +0.013$, all within their
across-seed SDs. Melanocytic nevi is held in equal proportion by
every client, leaving no inter-client drift for the proximal
anchor to act on; $\Delta = +0.002$ with both algorithms at
$F_1 \approx 0.885 \pm 0.004$, the within-experiment per-class
control predicted by the partition design.
Figure~\ref{fig:engineered-per-class} confirms the pattern: the
melanoma panel shows the largest persistent gap across all 150
rounds (FedAvg plateaus near $0.20$, FedProx near $0.30$), and the
melanocytic-nevi panel shows two indistinguishable curves. The other
five class panels show modest or noisy differences without a
visually persistent gap.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_engineered_per_class.pdf}
\caption{Validation F1 over 150 communication rounds for each
DermaMNIST class on the engineered partition. Curves show mean
validation F1 across 10 paired seeds with $\pm$SEM bands. Final
test-set $\Delta$ values at the best-validation checkpoint are
reported separately in Table~\ref{tab:engineered-per-class}.}
\label{fig:engineered-per-class}
\end{figure}

\subsection{Precision and recall decomposition}
\label{sec:engineered-per-class-pr}

To isolate whether the melanoma F1 advantage reflects better
recall, better precision, or both, Table~\ref{tab:engineered-per-class-pr}
reports per-class precision, recall, and F1 for FedAvg and FedProx
on the engineered partition. The pure-PyTorch primary runner used
for Table~\ref{tab:engineered-per-class} does not persist per-sample
test predictions, so the decomposition is computed from the
Flower-runtime S0 baseline of the same experiment (engineered
\texttt{balanced\_paired\_7\_clients} partition, $n = 10$ paired
seeds, predictions at each seed's best-validation checkpoint). The
Flower S0 per-class F1 pattern is consistent with the pure-PyTorch
primary: melanoma exhibits the largest within-pair FedProx advantage
in both runtimes (Flower $\Delta = +0.068$, pure-PyTorch $\Delta =
+0.114$), and melanocytic nevi shows $\Delta \approx 0$ in both.

\begin{table}[!htbp]
\centering
\caption{Per-class precision, recall, and F1 for FedAvg and FedProx
on the engineered partition, computed from saved test-set predictions
at the best-validation checkpoint (Flower runtime, $n = 10$ paired
seeds, mean $\pm$ standard deviation across seeds). Bolding is
reserved for the melanoma row, where the within-pair differences
are the only ones load-bearing in the discussion that follows; for
all other classes the differences are within their across-seed SDs
and are reported descriptively. Reported on the Flower runtime
because the pure-PyTorch primary does not persist per-sample
predictions; the per-class F1 pattern is consistent with the
pure-PyTorch headline (Table~\ref{tab:engineered-per-class}).}
\label{tab:engineered-per-class-pr}
\footnotesize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.15}
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}l cc cc cc@{}}
\toprule
                              & \multicolumn{2}{c}{Precision}                             & \multicolumn{2}{c}{Recall}                                & \multicolumn{2}{c}{F1}                                    \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}\cmidrule(lr){6-7}
Class (prevalence)            & FedAvg                     & FedProx                      & FedAvg                     & FedProx                      & FedAvg                     & FedProx                      \\
\midrule
Actinic keratoses (3.27\%)    & $0.532 \pm 0.087$          & $0.498 \pm 0.062$            & $0.427 \pm 0.125$          & $0.480 \pm 0.073$            & $0.454 \pm 0.046$          & $0.483 \pm 0.042$            \\
Basal cell carcinoma (5.13\%) & $0.553 \pm 0.031$          & $0.575 \pm 0.039$            & $0.446 \pm 0.072$          & $0.398 \pm 0.075$            & $0.489 \pm 0.044$          & $0.466 \pm 0.056$            \\
Benign keratosis (10.97\%)    & $0.495 \pm 0.080$          & $0.540 \pm 0.059$            & $0.399 \pm 0.162$          & $0.368 \pm 0.053$            & $0.409 \pm 0.110$          & $0.432 \pm 0.030$            \\
Dermatofibroma (1.15\%)       & $0.383 \pm 0.094$          & $0.322 \pm 0.126$            & $0.300 \pm 0.134$          & $0.278 \pm 0.133$            & $0.304 \pm 0.075$          & $0.276 \pm 0.092$            \\
Melanoma (11.11\%)            & $0.597 \pm 0.134$          & $0.542 \pm 0.077$            & $0.161 \pm 0.093$          & $\mathbf{0.209 \pm 0.045}$   & $0.228 \pm 0.087$          & $\mathbf{0.296 \pm 0.045}$   \\
Melanocytic nevi (67.05\%)    & $0.826 \pm 0.010$          & $0.819 \pm 0.005$            & $0.955 \pm 0.009$          & $0.960 \pm 0.005$            & $0.886 \pm 0.003$          & $0.884 \pm 0.002$            \\
Vascular lesions (1.41\%)     & $0.697 \pm 0.146$          & $0.646 \pm 0.073$            & $0.738 \pm 0.078$          & $0.734 \pm 0.043$            & $0.704 \pm 0.053$          & $0.685 \pm 0.050$            \\
\bottomrule
\end{tabular}%
}
\end{table}

The melanoma F1 improvement is driven by recall: FedProx raises
recall from $0.161$ to $0.209$ ($\Delta = +0.048$, $7/10$ wins)
while precision falls from $0.597$ to $0.542$ ($\Delta = -0.055$).
For a screening-oriented interpretation this trade favours fewer
missed melanomas at the cost of more false positives; because the
model is a $28 \times 28$ benchmark without threshold calibration,
the result is a classification diagnostic, not a clinical
operating recommendation. Melanocytic nevi shows essentially
identical precision, recall, and F1 under both algorithms,
preserving the per-class control.

Melanoma and actinic keratoses are held by only two of seven
clients each, so inter-client drift exists for those classes and
the proximal anchor has something to constrain. Melanocytic nevi
is held in equal proportion by all seven clients, so no
inter-client drift exists for the anchor to act on, and the
empirical $\Delta$ is near zero. This within-experiment contrast
is the per-class evidence consistent with the drift-control
interpretation.

% ==============================================================================
\section{Robustness Across Partition Designs}
\label{sec:partition-robustness}
% ==============================================================================

To check that the FedProx-vs-FedAvg comparison reproduces beyond
the engineered design, the same paired protocol was run on three
alternative non-IID partitions on the Flower runtime with the
same ten seeds. The IID and engineered partitions remain as
reference rows from earlier sections.

\subsection{Partition designs}
\label{sec:partition-designs}

The two new partitions are visualised in
Figure~\ref{fig:partition-comparison-dir-spec}.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.98\textwidth]{F_partition_comparison_dir_spec.pdf}
\caption{The two new partition designs introduced for the
robustness analysis (seed $42$). (a) Dirichlet $\alpha = 0.1$:
class-by-class proportions drawn from a Dirichlet, producing
severe joint label-and-quantity skew (per-client totals range
$91$--$3{,}721$). (b) Specialist (1-of-7): every minority class
is held by \emph{exactly one} client, with per-client sample
totals matched to the engineered partition
(Figure~\ref{fig:engineered-partition}) so that the only
variable between the engineered and specialist designs is the
structural change from paired to singleton minority-class
ownership.}
\label{fig:partition-comparison-dir-spec}
\end{figure}

\paragraph{Dirichlet $\alpha = 0.1$.}
The standard non-IID benchmark partitioning of Hsu et al.\ (2019):
class proportions per client drawn from a Dirichlet. At $\alpha =
0.1$, per-client sample counts range $91$--$3{,}721$ and classes
concentrate on small subsets of clients (joint label + quantity
skew).

\paragraph{Specialist (1-of-7).}
Each minority class is held by \emph{exactly one} client (rather
than two as in the engineered design), with per-client totals
matched to the engineered partition. No inter-client drift exists
on any minority class, so this tests the contrapositive of the
engineered design's prediction.

\paragraph{Engineered balanced-paired (Flower).}
The engineered partition replicated on Flower, included for
within-runtime comparison.

\subsection{Results}
\label{sec:partition-robustness-results}

Table~\ref{tab:partition-robustness} reports test macro-F1
across the ten paired seeds on each partition. The IID baseline
from Section~\ref{sec:iid-baseline} is included as the reference
row. Figure~\ref{fig:partition-robustness} shows the per-round
validation macro-F1 trajectories on each partition.

\begin{table}[!htbp]
\centering
\caption{Robustness across four partition designs on the
Flower runtime, $n = 10$ paired seeds, all hyperparameters
held identical (only the partition function varies between
rows). Test macro-F1 reported as mean $\pm$ standard deviation
across seeds. No row is bolded because all four within-pair
differences are within the across-seed SDs of either algorithm
and are reported descriptively. The IID row is the no-drift
reference from Section~\ref{sec:iid-baseline}.}
\label{tab:partition-robustness}
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{@{}l c c r c@{}}
\toprule
Partition                  & FedAvg              & FedProx             & $\Delta$ & Wins   \\
\midrule
IID                        & $0.5846 \pm 0.0197$ & $0.5786 \pm 0.0190$ & $-0.006$ & $5/10$ \\
Dirichlet $\alpha = 0.1$   & $0.4796 \pm 0.0355$ & $0.4875 \pm 0.0341$ & $+0.008$ & $6/10$ \\
Specialist (1-of-7)        & $0.4365 \pm 0.0236$ & $0.4446 \pm 0.0172$ & $+0.008$ & $7/10$ \\
Engineered balanced-paired & $0.4964 \pm 0.0150$ & $0.5033 \pm 0.0206$ & $+0.007$ & $7/10$ \\
\bottomrule
\end{tabular}
\end{table}

The direction of the FedProx-vs-FedAvg effect is consistent
across the three non-IID partition designs (Dirichlet,
specialist, engineered): all three produce a positive mean
within-pair $\Delta$ in the range $+0.007$ to $+0.008$ macro-F1,
with FedProx winning the majority of paired seeds in each case
($6/10$, $7/10$, $7/10$). The IID partition produces the
opposite sign of small magnitude ($\Delta = -0.006$, FedProx
wins $5/10$). The four-way contrast between IID and the three
non-IID designs reproduces the qualitative pattern expected of
the proximal-anchor mechanism: positive direction under
non-IID, near-zero direction under IID. The magnitudes on
the three non-IID partitions are similar in size to the
across-seed standard deviations of either algorithm
($\sim 0.02$--$0.03$), so we report them as descriptive
observations of direction rather than as quantitatively
distinguishable effect sizes.

The absolute macro-F1 levels vary substantially across
partitions, reflecting differences in task difficulty: IID is
the easiest setting ($\sim 0.58$ macro-F1) because every client
sees the global class distribution proportionally; the
specialist partition is the hardest ($\sim 0.44$) because each
minority class is held by only one client and the global model
must aggregate over highly specialised local optima; the
Dirichlet $\alpha = 0.1$ and engineered partitions sit between
the two extremes. The within-pair $\Delta$ comparison in
Table~\ref{tab:partition-robustness} is paired-seed within each
row and therefore controls for these absolute-level differences
across partitions.

\paragraph{Outlier disclosure on the Dirichlet row.} One of the
ten Dirichlet $\alpha = 0.1$ seeds (seed $8675309$) shows a
catastrophic FedProx training failure: FedAvg achieves $0.486$
macro-F1 while FedProx falls to $0.114$ (within-pair
$\Delta = -0.372$). Excluding this single seed, the Dirichlet
within-pair $\Delta$ becomes $+0.042$ with FedProx winning $9/10$
seeds. The aggregate $+0.008$ reported in
Table~\ref{tab:partition-robustness} retains all ten seeds for
transparency and is therefore conservative with respect to a
mean-difference effect; the variance reduction claim of
Section~\ref{sec:engineered-headline} does not apply on this
partition.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_partition_robustness.pdf}
\caption{Validation macro-F1 over $150$ communication rounds
on four DermaMNIST partition designs (Flower runtime, $n = 10$
paired seeds, mean $\pm$ SEM band). The IID panel (top-left)
shows the two algorithms' trajectories essentially
superimposed; the three non-IID panels (top-right, bottom-left,
bottom-right) show a small but visible persistent gap with
FedProx above FedAvg from approximately round~$30$ onwards.}
\label{fig:partition-robustness}
\end{figure}

\subsection{Cross-design observation}
\label{sec:partition-cross-design}

The three non-IID partitions produce within-pair $\Delta$ values
within $\pm 0.001$ macro-F1 of each other ($+0.007$, $+0.008$,
$+0.008$), all small relative to their across-seed SDs ($\sim
0.02$--$0.03$). At $n = 10$, the aggregate effect is not strongly
partition-specific under Flower.

A more discriminating contrast emerges per-class. On the melanoma
class, the engineered partition gives $\Delta_{\mathrm{melanoma}} =
+0.068$ ($7/10$ wins) while the specialist partition gives
$\Delta_{\mathrm{melanoma}} = -0.015$ ($4/10$ wins). Engineered
holds melanoma on two clients (drift between them); specialist
holds it on one (no inter-client drift), consistent with the
interpretation that the proximal anchor's contribution is
conditional on multiple clients sharing the class.


% ==============================================================================
\section{Heterogeneity Ladder (Stage A pilot)}
\label{sec:heterogeneity-ladder}
% ==============================================================================

The partitions reported so far were chosen for their narrative role
(IID baseline, engineered specialty pair, dominant-vs-specialist
stress test). To put them on a single quantitative axis, this
section reports a controlled five-level heterogeneity ladder
indexed by the average per-client Jensen--Shannon divergence
between each client's empirical label distribution and the global
label distribution.\footnote{We use Jensen--Shannon divergence
rather than Earth Mover's Distance because the seven DermaMNIST
classes are categorical and unordered, leaving no canonical ground
metric for EMD (Zhao et al.\ 2018 use EMD on label-ordered MNIST).
JS divergence is symmetric, bounded in $[0, 1]$ with base-$2$
logarithms, and requires no class ordering.}
All five levels use $K = 2$ clients, the engineered
balanced-paired-rare-stress \emph{construction methodology} of
Section~\ref{sec:engineered-headline} adapted across heterogeneity
dimensions, identical hyperparameters, and the same global
validation and test sets.

\paragraph{Ladder construction.}
The five levels and their measured heterogeneity scalars are:

\begin{table}[!htbp]
\centering
\caption{Heterogeneity-ladder partition specifications. JS is the
average per-client Jensen--Shannon divergence to the global label
distribution; class overlap is the fraction of the seven DermaMNIST
classes shared by both clients.}
\label{tab:ladder-partitions}
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{@{}l l r r r@{}}
\toprule
Level & Partition & C0 / C1 sizes & JS divergence & Class overlap \\
\midrule
L0 & 50/50 stratified IID            & $3506$ / $3501$ & $0.000$ & $1.00$ \\
L1 & 86/14 quantity-only stratified  & $6048$ / $959$  & $0.000$ & $1.00$ \\
L2 & 50/50 label-skew-only           & $3503$ / $3504$ & $0.104$ & $0.14$ \\
L3 & 70/30 rare-enriched             & $4905$ / $2102$ & $0.121$ & $0.14$ \\
L4 & 86/14 severe rare-class stress  & $6049$ / $958$  & $0.385$ & $0.00$ \\
\bottomrule
\end{tabular}
\end{table}

Levels L0 and L1 share a JS divergence of effectively zero (both
are stratified) but differ on quantity skew ($50/50$ vs.\ $86/14$);
they isolate \emph{quantity} as a heterogeneity dimension. Levels
L2 and L3 introduce label skew at moderate severity with class
overlap collapsing from $1.00$ to $0.14$. Level L4 is the existing
engineered $90/10$ rare-class-disjoint partition used in
Section~\ref{sec:small-hospital}, here re-run on the Flower
runtime with three algorithms.

\paragraph{Results.}
Table~\ref{tab:ladder-results} reports test macro-F1 and
rare-class-average F1 for FedAvg, FedProx ($\mu = 0.01$), and
FedNova on each level. FedNova is run on L1, L3, and L4 only --- the
levels where the algorithm's normalised-aggregation rule
(Wang et al.\ 2020) is theoretically active. Figure~\ref{fig:ladder-macro-f1}
plots the same numbers against the JS divergence index and decomposes
the algorithmic effect as $\Delta$ macro-F1 relative to FedAvg.

\begin{table}[!htbp]
\centering
\caption{Heterogeneity-ladder Stage A pilot, seed $42$ only (single
seed). Test macro-F1 reported at the per-seed best-validation
checkpoint. Rare-avg-F1 is the mean of dermatofibroma, melanoma,
and vascular lesions F1. $\Delta$ columns are relative to FedAvg on
the same level.}
\label{tab:ladder-results}
\small
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{@{}l l c c c c c c c@{}}
\toprule
& & \multicolumn{3}{c}{Macro-F1} & \multicolumn{2}{c}{$\Delta$ vs.\ FedAvg} & \multicolumn{2}{c}{Rare-avg-F1} \\
\cmidrule(lr){3-5}\cmidrule(lr){6-7}\cmidrule(lr){8-9}
Level & JS & FedAvg & FedProx & FedNova & FedProx & FedNova & FedAvg & FedProx \\
\midrule
L0 & $0.000$ & $0.608$ & $0.605$ & ---     & $-0.003$ & ---      & $0.558$ & $0.545$ \\
L1 & $0.000$ & $0.588$ & $0.591$ & $0.593$ & $+0.002$ & $+0.005$ & $0.537$ & $0.546$ \\
L2 & $0.104$ & $0.549$ & $0.537$ & ---     & $-0.012$ & ---      & $0.477$ & $0.454$ \\
L3 & $0.121$ & $0.512$ & $0.509$ & $0.527$ & $-0.003$ & $+0.015$ & $0.424$ & $0.428$ \\
L4 & $0.385$ & $0.518$ & $0.492$ & $0.506$ & $-0.026$ & $-0.012$ & $0.388$ & $0.312$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_ladder_macro_f1.pdf}
\caption{Heterogeneity-ladder Stage A pilot (seed $42$, single
seed). \textbf{(a)} Test macro-F1 as a function of the average
per-client Jensen--Shannon divergence to the global label
distribution, one line per algorithm. Performance decreases
monotonically with JS divergence for all three algorithms; the
algorithm-rank ordering at each level is within $\pm 0.03$ of
FedAvg. \textbf{(b)} $\Delta$ macro-F1 relative to FedAvg on the
same level. FedNova exceeds FedAvg at $L1$ ($+0.005$) and $L3$
($+0.015$); FedProx does not exceed FedAvg at any level. The
largest single-method advantage in either direction is
$|\Delta| = 0.026$ (FedProx $-$ FedAvg at L4).}
\label{fig:ladder-macro-f1}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_ladder_per_class.pdf}
\caption{Per-class test F1 across the ladder for the three
algorithms (one panel each). Rare classes (dermatofibroma,
melanoma, vascular lesions) are shaded. Per-class F1 on rare
classes degrades steeply as JS divergence grows --- particularly
on dermatofibroma, where L4's $80$-sample training pool falls
entirely on Client 1 and the global model's recall collapses below
$0.1$ under FedAvg/FedProx. FedNova on L4 (yellow line, right
panel) reaches dermatofibroma $F_1 = 0.195$, partially recovering
the rare-class signal that the other two algorithms lose --- but
at the cost of vascular F1 ($0.567$ vs.\ $0.702$ for FedAvg).}
\label{fig:ladder-per-class}
\end{figure}

\paragraph{Reading the ladder.}
Three observations follow.

First, all three algorithms degrade monotonically with JS
divergence: macro-F1 falls from $\approx 0.60$ at $L0$ to $\approx
0.50$ at $L4$. The slope of this decline is approximately constant
across algorithms; no single algorithm holds up better as the
ladder severity grows.

Second, on this single seed FedProx does \emph{not} consistently
outperform FedAvg. The largest signed difference between FedProx
and FedAvg is $-0.026$ (FedProx \emph{worse} at L4). The
proximal regulariser's expected mechanism --- bounding inter-client
drift on highly heterogeneous partitions --- does not translate
into a macro-F1 advantage here. This is in tension with the
theoretical motivation of FedProx (Li et al.\ 2020) and with
NIID-Bench's (Li, Diao, Chen, He 2022) general observation that
the appropriate FL algorithm depends on the heterogeneity type ---
neither result claims FedProx is universally better.

Third, FedNova exceeds FedAvg at L1 and L3 by small but
consistent margins ($+0.005$ and $+0.015$), and falls below FedAvg
at L4 ($-0.012$). The L3 result is the largest single-method
advantage in the ladder; the L4 instability is consistent with
recent benchmark literature
(\emph{Not All Federated Learning Algorithms Are Created Equal},
2024) reporting FedNova as unstable under severe class imbalance.

\paragraph{Single-seed caveat.}
All numbers in this section are from a one-seed pilot. Section~\ref{sec:small-hospital}
documents that the same seed $42$ on a different HPC compute node
produced a different ranking on the $90/10$ partition. The
algorithm differences across the ladder (largest $|\Delta| =
0.026$) are within that observed seed-level variance ($\approx
0.04$ macro-F1). Multi-seed replication is required before the
algorithmic rank ordering reported here can support inferential
claims; the qualitative monotonic-degradation-with-JS-divergence
pattern is, however, robust to single-seed concerns and would be
the surviving claim under multi-seed promotion. The heterogeneity
quantification (JS divergence per level) is dataset-level and not
seed-dependent.

% ==============================================================================
\section{Case Study: A Small Site with Unique Rare Classes}
\label{sec:small-hospital}
% ==============================================================================

The partitions reported so far hold all classes on at least one
numerically substantial client. In real federations, a frequent
deployment pattern is the opposite: a small specialist site holds
rare cases that no other site has seen. To probe how FedProx behaves
in this regime, we ran the canonical 2-client 90/10 partition
\texttt{two\_client\_90\_10\_rare\_stress}. Client~0 (``general
hospital'', $n = 6{,}049$, $\approx 86\%$ of the training set) holds
all actinic keratoses, basal cell carcinoma, benign keratosis, and
melanocytic nevi samples and zero rare-class samples. Client~1
(``specialist site'', $n = 958$, $\approx 14\%$) holds all
dermatofibroma, melanoma, and vascular lesion samples and nothing
else. Hyperparameters and training protocol are identical to the
engineered headline; the Flower runtime is used.

This experiment is reported as a single-seed case study
($\text{seed} = 42$) and is descriptive only --- the goal is to
illustrate the mechanism direction in a deliberately stressed
configuration, not to provide a paired-seed inferential estimate.

\begin{table}[!htbp]
\centering
\caption{Test performance under the 2-client 90/10 rare-stress
partition (Flower runtime, seed~$42$, single-seed case study).
Per-class F1 is reported on the global test set at the
best-validation checkpoint. The starred rows are the rare classes
held only by Client~1 (the small site). $\Delta$ is FedProx
$-$ FedAvg; the higher value per row is shown in bold.}
\label{tab:small-hospital}
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{@{}l c c r@{}}
\toprule
Metric                                       & FedAvg            & FedProx           & $\Delta$          \\
\midrule
Macro-F1                                     & $0.478$           & $\mathbf{0.509}$  & $\mathbf{+0.031}$ \\
Overall accuracy                             & $0.738$           & $\mathbf{0.744}$  & $+0.006$          \\
\midrule
\multicolumn{4}{l}{\textit{Per-class F1 --- classes on Client 0 (large, $86\%$):}} \\
\quad Actinic keratoses                      & $\mathbf{0.507}$  & $0.483$           & $-0.024$          \\
\quad Basal cell carcinoma                   & $0.492$           & $\mathbf{0.517}$  & $+0.025$          \\
\quad Benign keratosis                       & $\mathbf{0.529}$  & $0.526$           & $-0.003$          \\
\quad Melanocytic nevi                       & $\mathbf{0.887}$  & $0.882$           & $-0.005$          \\
\midrule
\multicolumn{4}{l}{\textit{Per-class F1 --- rare classes on Client 1 (small, $14\%$):}} \\
\quad Dermatofibroma~$\bigstar$              & $0.079$           & $\mathbf{0.233}$  & $\mathbf{+0.153}$ \\
\quad Melanoma~$\bigstar$                    & $0.313$           & $\mathbf{0.324}$  & $+0.011$          \\
\quad Vascular lesions~$\bigstar$            & $0.537$           & $\mathbf{0.596}$  & $\mathbf{+0.059}$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_small_hospital_per_class.pdf}
\caption{Per-class test F1 under the 2-client 90/10 rare-stress
partition (Flower runtime, single seed). The starred classes are
held only by Client~1 (the small site, $14\%$ of training data) and
are highlighted by the shaded bands; the unshaded classes are held
only by Client~0 (the large site, $86\%$). The largest single-class
effect is on dermatofibroma, whose F1 nearly triples from
$0.079$ (FedAvg) to $0.233$ (FedProx).}
\label{fig:small-hospital}
\end{figure}

The effect is localised on the small site's classes. The two largest
F1 differences ($+0.153$ on dermatofibroma, $+0.059$ on vascular
lesions) both occur on classes that exist only on Client~1; the
classes held by the dominant Client~0 move within $\pm 0.025$ of the
FedAvg baseline. The mechanism is consistent with the size-weighted
aggregation rule: at $n_0 / n_1 \approx 6.3$, the dominant client's
gradient signal carries roughly $86\%$ of the aggregated update, and
the small client's rare-class signal is correspondingly muted under
FedAvg. The proximal regulariser restrains how far the dominant
client can move from the broadcast global parameters within each
round, which leaves a comparatively larger share of the aggregated
update available to incorporate the small client's rare-class
contribution.

Because this is a single seed, the absolute magnitudes should be
read with caution and the dermatofibroma effect in particular sits
on a base of only $\approx 110$ test samples. The
\emph{direction} of the effect is, however, the one predicted by the
mechanism analysed in Sections~\ref{sec:engineered-per-class}
and~\ref{sec:update-norms}: FedProx's proximal term acts on
inter-client drift, and a 2-client 90/10 partition produces drift
between exactly the kind of dominant generalist and small
rare-class specialist that motivates federated learning in
multi-institution medical deployments.

\subsection{The value of federation, decomposed by client}
\label{sec:small-hospital-federation-value}

The preceding subsection compared two federated algorithms on the
same data. A complementary question, and one of the most operationally
important in federated medical AI (Sheller et al.\ 2018, 2020;
Roth et al.\ 2020; Pati et al.\ 2022), is what each client \emph{gains
by participating in the federation at all} relative to training only on
its own local data. To make this contrast concrete, each client was
trained alone on its own 86\% / 14\% partition using a compute budget
matched to the federation (3{,}000 epochs of local SGD, the same
cumulative SGD-step total as $R = 150$ rounds of $E = 20$ local epochs).
Identical hyperparameters, identical global validation and test sets,
identical best-validation-checkpoint protocol. The local-only runs are
therefore directly comparable to the federated and centralised
references already reported.
Table~\ref{tab:small-hospital-fed-value} and
Figure~\ref{fig:federation-value} report the five-way contrast;
Figure~\ref{fig:per-client-specialty-curves} additionally shows the
$150$-round federated trajectories on each client's specialty
classes, with the local-only reference drawn as a horizontal dashed
line in each panel.

\begin{table}[!htbp]
\centering
\caption{Test performance under five training regimes on the 2-client
90/10 partition (Flower runtime, seed~$42$, single-seed case study).
Per-class F1 columns are abbreviated to their first letter (A=actinic
keratoses, B=basal cell carcinoma, BK=benign keratosis,
\textbf{D}=dermatofibroma, \textbf{M}=melanoma, N=melanocytic nevi,
\textbf{V}=vascular lesions). The starred classes (D, M, V) are held
only by Client~1.}
\label{tab:small-hospital-fed-value}
\footnotesize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.15}
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}l c c c c c c c c c@{}}
\toprule
Regime                       & Macro-F1 & A & B & BK & D$^{\bigstar}$ & M$^{\bigstar}$ & N & V$^{\bigstar}$ & acc.\ \\
\midrule
Client 0 alone (local-only)  & $0.349$  & $0.504$ & $0.533$ & $0.522$ & $0.000$           & $0.000$           & $0.883$ & $0.000$           & $0.747$ \\
Client 1 alone (local-only)  & $0.123$  & $0.000$ & $0.000$ & $0.000$ & $0.106$           & $0.236$           & $0.000$ & $0.520$           & $0.129$ \\
FedAvg federated             & $\mathbf{0.500}$ & $\mathbf{0.523}$ & $0.513$          & $\mathbf{0.534}$ & $\mathbf{0.232}$ & $\mathbf{0.369}$ & $0.869$          & $\mathbf{0.459}$ & $0.721$ \\
FedProx federated ($\mu = 0.01$) & $0.458$          & $0.511$          & $\mathbf{0.516}$ & $0.513$          & $0.100$          & $0.301$          & $\mathbf{0.887}$ & $0.379$          & $0.745$ \\
FedAvg + Client 0 fine-tune  & $0.429$          & $0.534$          & $\mathbf{0.567}$ & $0.512$          & $0.074$          & $0.043$          & $0.886$          & $0.389$          & --      \\
FedAvg + Client 1 fine-tune  & $0.182$          & $0.059$          & $0.038$          & $0.000$          & $0.199$          & $0.230$          & $0.189$          & $\mathbf{0.559}$ & --      \\
FedProx + Client 0 fine-tune & $0.383$          & $0.516$          & $0.537$          & $0.508$          & $0.083$          & $0.034$          & $0.880$          & $0.121$          & --      \\
FedProx + Client 1 fine-tune & $0.135$          & $0.000$          & $0.000$          & $0.000$          & $0.185$          & $0.219$          & $0.000$          & $0.543$          & --      \\
Centralised (pooled)         & $0.555$          & $0.433$          & $0.548$          & $0.576$          & $0.320$          & $0.415$          & $\mathbf{0.891}$ & $\mathbf{0.704}$ & $0.779$ \\
\bottomrule
\end{tabular}%
}
\end{table}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_federation_value.pdf}
\caption{Decomposed federation value on the 2-client 90/10 partition
(Flower runtime, seed~$42$). \textbf{(a)} Test macro-F1 across the
five training regimes. The dashed line marks the centralised
reference; the two arrows annotate each client's macro-F1 lift from
local-only training to FedProx federated training. \textbf{(b)}
Per-class test F1 with rare classes (held only by Client~1) marked
$\bigstar$ and shaded.}
\label{fig:federation-value}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_per_client_specialty_curves.pdf}
\caption{Per-client specialty trajectories on the 2-client 90/10
partition (Flower runtime, seed~$42$, single seed). For each panel,
the mean validation F1 over the client's own classes is plotted
across the $150$ communication rounds for FedAvg (green) and FedProx
(blue); thin transparent lines show the raw per-round trajectory and
thick lines a $9$-round centred moving average. The dashed gold
reference line marks the local-only best-validation F1 averaged over
the same classes (the value the client achieves training alone, with
matched compute budget). \textbf{(a)} Client~0's specialty --- the
common classes actinic keratoses, basal cell carcinoma, benign
keratosis, and melanocytic nevi --- on which federation
\emph{slightly under-performs} the local-only baseline ($\approx$
$0.61$--$0.62$ federated vs.\ $0.658$ local-only). \textbf{(b)}
Client~1's specialty --- the rare classes dermatofibroma, melanoma,
and vascular lesions --- on which federation \emph{substantially
outperforms} the local-only baseline ($\approx 0.39$--$0.47$
federated vs.\ $0.282$ local-only). At the best-validation rounds
chosen for the reported test scores (FedAvg round $150$, FedProx
round $146$), the mean over Client~1's classes is $0.388$ for
FedAvg and $0.472$ for FedProx --- the latter consistent with the
test-set per-class advantage on dermatofibroma reported in
Table~\ref{tab:small-hospital-fed-value}.}
\label{fig:per-client-specialty-curves}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.78\textwidth]{F_pareto_small_hospital.pdf}
\caption{Federation Pareto frontier on the 2-client 90/10 partition
(Flower runtime, seed~$42$). Each point's $x$-coordinate is the test
F1 averaged over Client~0's four classes (actinic, basal, benign
keratosis, melanocytic nevi); its $y$-coordinate is the test F1
averaged over Client~1's three rare classes (dermatofibroma,
melanoma, vascular). The two local-only points lie on opposite axes
because the class-disjoint partition forces each client to score
$F_1 = 0$ on classes it has never observed. Federated points
($\text{FedAvg}, \text{FedProx}$) lie on the trade-off frontier
between the two local-only corners. The dashed arrows mark each
client's lift from local-only to FedProx. Centralised training
($\approx (0.61, 0.48)$) is the Pareto-optimal point; FedProx
$\approx (0.60, 0.38)$ is the closest federated approximation, and
FedAvg $\approx (0.60, 0.31)$ is strictly Pareto-dominated by FedProx
on this partition. The diagonal $y = x$ line marks the
``equal-specialty'' configuration where both clients are served
equally well; centralised falls below this line, indicating that even
under pooled training the rare classes are harder than the common
ones.}
\label{fig:pareto-small-hospital}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_rare_class_confusion.pdf}
\caption{Confusion-row decomposition for Client~1's three rare
classes, comparing FedAvg and FedProx at the best-validation
checkpoint. Each horizontal bar is the distribution of test-set
predictions for the indicated true class; colours encode the
predicted class. Numbers in parentheses on the left are the count of
correctly identified test samples out of the total for that class.
On dermatofibroma ($n = 23$) FedAvg correctly identifies $8/23$
test samples ($35\%$); FedProx correctly identifies $2/23$ ($9\%$),
with predictions defaulting to nevi. On melanoma ($n = 223$) FedAvg
recovers $94/223$ ($42\%$) and FedProx $54/223$ ($24\%$); under
FedProx, $53\%$ of melanoma test cases are misclassified as nevi,
compared with $35\%$ under FedAvg. On vascular lesions ($n = 29$)
FedAvg recovers $17/29$ ($59\%$) and FedProx $11/29$ ($38\%$). Note:
these numbers reflect the latest seed-$42$ re-run and are in
tension with the previous run on the same seed (see the
reproducibility caveat in the next subsection).}
\label{fig:rare-class-confusion}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_personalisation_gap.pdf}
\caption{Personalisation-gap analysis under $5$ epochs of local
fine-tuning at $\text{lr} = 0.001$ (Yu et al.\ 2022 protocol). Each
panel reports the per-class test F1 of the federated global model
(green, pre-FT) and the same model after $5$ epochs of local SGD on
one client's data (red, post-FT). Δ on top of each panel is the
post-FT macro-F1 change. All four configurations produce
\emph{negative} personalisation gaps, with severity ranging from
$-0.07$ (fine-tuning on the large client's data) to $-0.32$
(fine-tuning on the small client's class-disjoint subset). The
post-FT model catastrophically forgets the classes the fine-tuning
client did not see, driving per-class $F_1$ on those classes to
near-zero (panels b and d, columns 0--2 and 5).}
\label{fig:personalisation-gap}
\end{figure}

Five observations follow.

\paragraph{Solo training is structurally inadequate, especially for
the small site.}
Client~0 alone reaches macro-F1 $0.349$ and Client~1 alone reaches
$0.123$ --- the latter is \emph{below} the $1/7 \approx 0.143$
chance level for a 7-class classifier. Both numbers are mechanically
explained by the class-disjoint partition: a client never assigns
non-zero F1 to a class it has never seen at training time. Client~0
scores exactly $F1 = 0$ on dermatofibroma, melanoma, and vascular
lesions; Client~1 scores exactly $F1 = 0$ on actinic keratoses,
basal cell carcinoma, benign keratosis, and (critically) melanocytic
nevi --- the $67\%$ majority class in the test set, which Client~1
has never observed. Increasing the compute budget does not address
this gap: Client~1's best-validation checkpoint is reached at training
epoch $40$ out of $3{,}000$, indicating that a $958$-sample
$3$-class local model overfits in $\approx 1.3\%$ of the federation's
per-client compute budget. The remainder of solo training degrades
the model rather than improving it. The bottleneck is data diversity,
not training time.

\paragraph{Federation lifts both clients, but lifts the small site
disproportionately.}
Federation raises Client~0's macro-F1 from $0.349$ (local-only) to
$0.500$ (FedAvg, $+0.15$) or $0.458$ (FedProx, $+0.11$), a relative
gain of $31$--$43\%$. The same federation raises Client~1's macro-F1
from $0.123$ to the same shared global model value, a relative gain
of $272$--$306\%$. Client~1 therefore captures roughly $2.4\times$
as much absolute macro-F1 by joining the federation as Client~0
does. This asymmetry is the empirical answer to the small-hospital
question: federated learning is most valuable to participants who
could not have learned the task alone, and the value comes
specifically from access to the classes their data does not contain.
The asymmetry is preserved across both federated algorithms and is
the only finding in this section that is robust to the
reproducibility caveat documented below.

\paragraph{Centralisation remains a soft ceiling but is
operationally inaccessible.}
The centralised reference at seed $42$ achieves macro-F1 $0.555$
($0.560 \pm 0.024$ averaged over the ten seeds of
Section~\ref{sec:centralised-baseline}). FedAvg recovers
$0.500 / 0.555 \approx 90\%$ of this; FedProx recovers $83\%$. The
remaining gap to centralisation is the cost of not pooling data
between clients; in the clinical setting that motivates this thesis
this gap is exactly the price of preserving institutional data
sovereignty, and is comparable in magnitude to the standard
deviation of the centralised reference itself.

\paragraph{The FedAvg-vs-FedProx ranking on this partition is
within seed-level noise.}
On the present (re-run) seed-$42$ outputs reported in
Table~\ref{tab:small-hospital-fed-value}, FedAvg outperforms FedProx
by $\Delta = +0.042$ macro-F1, with the FedAvg advantage
concentrated on rare classes (dermatofibroma $F_1 = 0.232$ vs.\
$0.100$; vascular $0.459$ vs.\ $0.379$). An earlier seed-$42$ run
on a different HPC compute node, executed with identical
hyperparameters before the runner was modified to also save the
best-validation model state, produced the \emph{opposite} ranking
(FedAvg $0.478$, FedProx $0.509$, $\Delta = -0.031$ in FedProx's
favour with dermatofibroma rising from $0.079$ to $0.233$ under
FedProx). The two runs disagree by $\approx 0.04$ macro-F1 on the
same seed --- a magnitude comparable to the algorithm difference
itself --- consistent with CUDA non-determinism across
\texttt{A100} compute nodes (each Flower run aggregates over $7$
clients $\times$ $150$ rounds of asynchronous-actor return order
and parameter serialisation; the compounding floating-point
non-associativity at $423{,}175$ parameters is enough to flip the
ranking on this partition).
Figure~\ref{fig:pareto-small-hospital} therefore reflects the
\emph{latest} run only; under multi-seed replication (Stage B,
not yet executed) the Pareto positions of FedAvg and FedProx are
expected to overlap rather than one dominating the other. The
algorithmic conclusion that survives the reproducibility caveat is
that \emph{FedAvg and FedProx are statistically indistinguishable on
this 90/10 partition at $n = 1$ seed}; the federation-vs-local
conclusion is unaffected and remains directionally robust.

\paragraph{Local fine-tuning produces negative personalisation
gaps on this class-disjoint partition (catastrophic forgetting).}
The four fine-tuning configurations reported in
Table~\ref{tab:small-hospital-fed-value} and visualised in
Figure~\ref{fig:personalisation-gap} all produce \emph{negative}
personalisation gaps relative to their federated starting point.
Fine-tuning on Client~$0$'s data (common classes only) drops macro-F1
from $0.500$ to $0.429$ (FedAvg) or from $0.458$ to $0.383$ (FedProx),
a loss of $\approx 0.07$--$0.08$ macro-F1; fine-tuning on
Client~$1$'s data (rare classes only) is catastrophic, dropping
macro-F1 to $0.182$ (FedAvg) or $0.135$ (FedProx) --- below the
local-only Client~$1$ baseline itself. The dominant failure mode is
catastrophic forgetting: $5$ epochs of local SGD at $\text{lr} =
0.001$ on a class-disjoint subset is enough to drive per-class
$F_1$ on \emph{unseen} classes to near-zero. On macro-F1 averaged
across the global $7$-class test set, this is reported as a single
large negative number, but the per-class breakdown reveals it is
specifically the classes the fine-tuning client did not see that
collapse, not all classes uniformly.
\textbf{Implication:} naive local fine-tuning under the Yu et al.\
(2022) protocol is \emph{not} a defensible personalisation strategy
for class-disjoint partitions of this severity. Personalised-FL
methods that preserve shared representation (FedRep, Collins et al.\
2021; Ditto, Li et al.\ 2021; pFedMe, Dinh et al.\ 2020) would be
the principled next step --- treated in this thesis as future work.

\paragraph{Federation as a mutual-aid contract.}
What is exchanged through the federated aggregator is not data but
\emph{class signal}: each client's local gradient update encodes
information about classes the other client has never seen, and
size-weighted parameter averaging mixes these signals into a
global model that respects both contributors. Under this framing,
the small-hospital scenario is best read as a contract --- both
sites accept a residual gap to centralised performance ($\approx
0.05$--$0.10$ macro-F1) in exchange for the federation supplying
the rare-class signal that neither site could obtain alone. The
clinical analogue is direct: a specialist hospital's rare-case
expertise becomes available to generalist hospitals through the
global model, and the generalist hospitals' broad common-case
coverage becomes available to the specialist --- without either
site sharing patient images.

The single-seed nature of this comparison remains the principal
caveat. Per-class scores --- particularly on the rare-class side
(dermatofibroma $n = 80$ globally, vascular lesions $n = 99$) ---
should be read for direction rather than magnitude. The
local-only-versus-federated direction is, however, robust by
construction: a client with zero training samples of class $c$
provably scores $F1 = 0$ on class $c$, and no amount of additional
compute or seed variation changes that. Federation is the
only mechanism in this setup that lifts either client off that
mechanical zero.

% ==============================================================================
\section{Parameter-Level Evidence: Per-Client Update Norms}
\label{sec:update-norms}
% ==============================================================================

The previous sections argued for the proximal-anchor
interpretation through outcome-level evidence (cross-partition
direction consistency, per-class melanoma vs mel-nevi contrast,
IID null). The $\mu$-sweep of Section~\ref{sec:mu-sensitivity}
showed that local-update shrinkage scales smoothly with $\mu$ on
the engineered partition. This section pins the parameter-level
result at the operating $\mu = 0.01$ to the role of inter-client
drift, by contrasting per-client update norms between the
engineered non-IID partition and the IID partition under
identical hyperparameters. If the proximal term acts on
inter-client drift, the FedAvg-to-FedProx update-norm ratio
should be larger on the non-IID partition than on IID. Smaller
updates under FedProx are consistent with drift control but do
not uniquely distinguish it from broader local-update
regularisation.

\subsection{What is measured}
\label{sec:update-norms-setup}

For each communication round $t$ and each client $i$, we log
the Euclidean distance between that client's locally-updated
parameter vector at the end of the round and the global
parameter vector broadcast at the start of the round:
\[
\rho_{t,i} \;=\; \lVert w_i^{t+1} - w^t \rVert_2.
\]
This quantity has a clean mechanism interpretation: under
FedAvg, $\rho_{t,i}$ is determined entirely by the local
training trajectory; under FedProx, the proximal regulariser
explicitly penalises $\lVert w - w^t \rVert$ during local
training, so $\rho_{t,i}$ is expected to be smaller. The
proximal anchor's drift-control effect, if present, should
appear directly in this measurement, independent of any
outcome-level metric.

The Flower runtime client-side training code logs $\rho_{t,i}$
for every (round, client) pair when the
\texttt{--log-update-norms} flag is enabled. This was the case
for the Flower engineered-partition baseline sweep
(\texttt{flower\_C0\_baseline}) and the Flower IID baseline
sweep (\texttt{flower\_C0\_iid\_baseline}); the remaining
Flower statistical-heterogeneity sweeps used in
Section~\ref{sec:partition-robustness} do not have update-norm
logging in their result directories. We therefore report
update-norm results on the engineered and IID partitions, the
two endpoints of the heterogeneity-intensity axis.

\subsection{Results}
\label{sec:update-norms-results}

Table~\ref{tab:update-norms} reports the mean update norm
across all (round, client) pairs, averaged across paired seeds,
for FedAvg and FedProx on each partition.
Figure~\ref{fig:update-norms} shows the per-round trajectory of
the mean update norm across the 150 communication rounds.

\begin{table}[!htbp]
\centering
\caption{Per-client update norm
$\lVert w_i^{t+1} - w^t \rVert_2$ averaged across all (round,
client) pairs and across the retained update-norm logs (Flower
runtime). The engineered row averages over 9 FedAvg and 8 FedProx
seeds (subset of the 10 paired seeds used for the macro-F1 result);
the IID row averages over all 10 FedAvg and 10 FedProx seeds. The
ratio column reports the mean FedAvg norm divided by the mean
FedProx norm for each partition.}
\label{tab:update-norms}
\small
\begin{tabular}{lccc}
\toprule
Partition & FedAvg $\lVert \Delta w \rVert$ & FedProx $\lVert \Delta w \rVert$ & Ratio (FA / FP) \\
\midrule
Engineered (non-IID)   & $1.523$ & $\mathbf{1.029}$ & $\mathbf{1.48\times}$ \\
IID (no-drift reference) & $0.825$ & $\mathbf{0.724}$ & $1.14\times$ \\
\bottomrule
\end{tabular}
\end{table}

Two observations follow from Table~\ref{tab:update-norms}.

First, on both partitions FedProx produces smaller per-client
update norms than FedAvg --- the direction predicted by the
proximal regulariser. This is a parameter-level signature of
local-update shrinkage: when the proximal regulariser is
active, per-round client updates are bounded in magnitude.

Second, the magnitude of the FedAvg/FedProx ratio differs
substantially between the two partitions. On the engineered
non-IID partition the FedAvg update norm is $1.48\times$ that of
FedProx ($1.523$ vs $1.029$); on the IID partition the ratio
shrinks to $1.14\times$ ($0.825$ vs $0.724$). The same
proximal coefficient $\mu = 0.01$ produces a substantially
stronger empirical drift-control effect on the non-IID
partition than on the IID partition. This is mechanism-
consistent: under IID, all clients are drifting toward
similar local optima (because their local objectives are
i.i.d.\ samples of the same global distribution), so there is
little client drift to constrain and the proximal anchor's
empirical effect is modest. Under the engineered non-IID
partition, clients drift toward distinct specialised local
optima, and the proximal anchor's effect is substantially
larger.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_update_norms.pdf}
\caption{Per-client update norm
$\lVert w_i^{t+1} - w^t \rVert_2$ across $150$ communication
rounds on the Flower runtime, averaged across clients within
each round and across paired seeds (mean $\pm$ SEM band). On
the engineered non-IID partition (left), FedProx maintains a
visibly smaller per-client update norm than FedAvg from
round~$1$ onwards; the gap persists across the entire $150$-
round horizon. On the IID partition (right), both algorithms'
update norms decay rapidly toward zero by round~$\sim 80$ and
the FedProx-FedAvg gap is correspondingly smaller. The cross-
partition contrast shows the proximal anchor's empirical effect
scales with the amount of inter-client drift present in the
training setup.}
\label{fig:update-norms}
\end{figure}

The convergence-time profile of the update norms also differs
between partitions. On the IID partition, both algorithms'
update norms decay to near zero by round $\sim 80$, indicating
that all seven clients have approximately converged to the
same global optimum and per-round changes are minimal. On the
engineered partition, the update norms decay more slowly and
remain non-trivial across the entire $150$-round horizon ---
clients continue to make meaningful per-round parameter
changes even in late training because their local optima
differ from the global aggregate. The FedAvg/FedProx ratio
remains stable across the late-training horizon on the
engineered partition rather than collapsing toward one,
indicating that the proximal regulariser remains active
throughout training rather than becoming inert at convergence.

\subsection{Trajectory-volatility corroboration}
\label{sec:update-norms-volatility}

An outcome-level signal that corroborates the parameter-level
finding is the per-round volatility of the validation
macro-F1 trajectory. Smaller per-client parameter updates
should produce smoother round-to-round changes in the global
model and therefore smoother validation curves; larger
updates should produce more volatile trajectories. We
quantify this by the mean absolute round-to-round change
in validation macro-F1 across the $150$ communication rounds,
averaged across the ten paired seeds of the pure-PyTorch
primary sweep:
\[
\text{volatility}_a \;=\;
\mathrm{mean}_r \big|\mathrm{F1}_a(r) - \mathrm{F1}_a(r-1)\big|.
\]
On the pure-PyTorch engineered headline,
$\text{volatility}_{\mathrm{FedAvg}} = 0.0414$ and
$\text{volatility}_{\mathrm{FedProx}} = 0.0225$, with the
FedAvg/FedProx ratio of $1.84\times$. Crucially, the ratio
exceeds one on every one of the ten paired seeds (per-seed
range: $1.30\times$ to $2.95\times$), so this is not a feature
of any single seed. FedProx's smoother validation trajectory
is a downstream consequence of its smaller per-client update
norm.

% ==============================================================================
\section{Performance gap relative to centralised training}
\label{sec:federation-tax}
% ==============================================================================

To contextualise the within-federation FedAvg-vs-FedProx
comparison against the absolute scale set by
Section~\ref{sec:centralised-baseline}, we express each
federated configuration's mean test macro-F1 as a percentage of
the centralised reference ($0.5600$ macro-F1). The four
federated configurations all use the engineered
\texttt{balanced\_paired\_7\_clients} partition, $n = 10$ paired
seeds, and identical hyperparameters.

\begin{table}[!htbp]
\centering
\caption{Performance gap relative to centralised training. Each
row's recovery percentage is its test macro-F1 mean divided by the
centralised baseline mean of $0.5600$.}
\label{tab:federation-tax}
\small
\begin{tabular}{lcc}
\toprule
Configuration & Test macro-F1 & Recovery vs centralised \\
\midrule
Centralised (non-federated)         & $\mathbf{0.5600 \pm 0.0240}$ & $100.0\%$ \\
FedAvg, pure-PyTorch                & $0.4814 \pm 0.0254$ & $86.0\%$ \\
FedProx, pure-PyTorch               & $0.5081 \pm 0.0140$ & $90.7\%$ \\
FedAvg, Flower                      & $0.4964 \pm 0.0150$ & $88.6\%$ \\
FedProx, Flower                     & $0.5033 \pm 0.0206$ & $89.9\%$ \\
\bottomrule
\end{tabular}
\end{table}

The performance gap between centralised training and any
federated configuration --- the ``federation tax,'' to use the
informal shorthand --- is $9$--$14$ percentage points of macro-F1,
substantially larger than the within-federation FedProx-vs-FedAvg
improvement ($4.7$ percentage points on pure-PyTorch, $1.3$ on
Flower). The thesis result is therefore best framed not as
``federated training matches centralised'' but as ``federated
FedProx closes a modest portion of the gap to centralised that
federated FedAvg leaves open.'' The remaining $9$--$10$
percentage-point gap to centralised is the cost of federation
itself and is not addressed by the choice of FL algorithm.

% ==============================================================================
\section{Computational-Work Heterogeneity: Motivation and Scope}
\label{sec:system-het-motivation}
% ==============================================================================

The chapter up to this point has focused on \emph{statistical}
heterogeneity --- differences in the data distribution held by
each client. Federated-learning theory and practice identifies
a second canonical axis of heterogeneity, equally important in
production deployments: \emph{system} heterogeneity, in which
clients differ in the amount of local computation they can
contribute per communication round. In real federated
deployments, institutions run on heterogeneous hardware, have
different on-call training windows, and cannot all complete the
same amount of local work within a round's wall-clock deadline.
Some clients (``stragglers'') return partial updates after fewer
local optimisation steps; the question is how the FL algorithm
should handle these partial updates.

The proximal term in FedProx was originally motivated for this
regime. Li et al.\ (2020), Theorem~4, proves a convergence
guarantee for FedProx under \emph{$\gamma$-inexact} local
updates: each client may submit a parameter vector
$w_i^{t+1}$ that does not fully minimise its local objective,
provided the residual inexactness is bounded. The proximal
regulariser $(\mu/2)\lVert w - w^t \rVert^2$ constrains how far
an inexact local update can drift from the broadcast global
anchor $w^t$, making partial work from stragglers usable for
aggregation without destabilising the global model. FedAvg has
no such guarantee, and its size-weighted aggregation rule
treats partial-work and full-work updates symmetrically,
weighted only by dataset size.

This chapter tests whether the FedProx-vs-FedAvg pattern
established under statistical heterogeneity in the preceding
sections persists, attenuates, or amplifies when clients have
heterogeneous per-round local-epoch budgets. The same
engineered \texttt{balanced\_paired\_7\_clients} partition,
the same ten paired seeds, and the same Flower simulation
runtime are used throughout, so that the only variable
between this chapter's experiments and the engineered
non-IID baseline of Section~\ref{sec:engineered-headline} is
the per-(round, client) local-epoch allocation. An IID
cross-partition control mirrors the contrast structure used in
Section~\ref{sec:partition-robustness}.

\paragraph{Scope and terminology.}
We use ``system heterogeneity'' as shorthand for what is
strictly \emph{computational-work heterogeneity}: variability
in the number of local SGD steps each client performs per
round. We do not simulate hardware speed, network latency, or
device availability; the manipulation acts only on the
algorithm via heterogeneous per-(round, client) local-epoch
budgets $\tau_i$. We restrict scope to the FedAvg/FedProx
comparison; alternative algorithms designed specifically for
heterogeneous local-step regimes are out of scope for this
chapter.

% ==============================================================================
\section{Experimental Design and Conditions}
\label{sec:system-het-design}
% ==============================================================================

Three computational-work heterogeneity conditions are evaluated,
denoted $S_0$, $S_1$, and $S_2$ (the $S$ prefix distinguishes the
condition labels from the client labels $C_0,\dots,C_6$ used in
the partition design). Each condition defines a per-(round,
client) local-epoch schedule that is fully determined by the
random seed; paired FedAvg/FedProx runs at the same seed therefore
see an identical schedule and the within-pair $\Delta$ reflects
only the algorithmic gating of the proximal term. The schedules
under seed~$42$ are visualised in Figure~\ref{fig:sh-schedules}.
Each condition is run on \emph{both} the engineered partition
(primary results, Section~\ref{sec:sh-engineered-results}) and
the IID partition (isolation,
Section~\ref{sec:sh-iid-isolation}) with the same ten paired
seeds, so the engineered-vs-IID contrast at each $S$ condition
quantifies the dependence of the proximal effect on
statistical heterogeneity.

\paragraph{S0 --- no computational-work heterogeneity (baseline).}
Every client performs $E = 20$ local epochs every round, for
all $150$ communication rounds. This is the same setting used
in the engineered-partition headline of
Section~\ref{sec:engineered-headline} and provides the
within-runtime within-partition baseline against which the
two system-heterogeneity conditions are compared. In the
schedule heatmap (Figure~\ref{fig:sh-schedules}, left), every
(round, client) cell is yellow, indicating the maximum
local-epoch budget.

\paragraph{S1 --- fixed stragglers.}
Two designated clients (C5 and C6) always perform
$E_{\text{straggler}} = 5$ local epochs; the remaining five
clients always perform the full $E = 20$. This represents the
deployment pattern in which specific institutions consistently
have weaker compute infrastructure or shorter training windows.
The schedule heatmap (Figure~\ref{fig:sh-schedules}, centre)
shows a solid vertical bar of $E = 5$ on columns C5 and C6
across all $150$ rounds, with the remaining columns at
$E = 20$.

\paragraph{Caveat on the S1 design.} On the engineered
\texttt{balanced\_paired\_7\_clients} partition, client~C5 is
part of the melanoma/vascular specialty pair (the pair that
drives the per-class melanoma advantage in
Section~\ref{sec:engineered-per-class}), and client~C6 is the
melanocytic-nevi-only generalist. S1 therefore couples the
``system heterogeneity'' manipulation with a specific clinical
role assignment under the engineered partition. The S2
condition below does not have this confound (the straggler
identity rotates randomly per round), so S2 is the cleaner
test of the pure system-heterogeneity effect. S1 is retained
for descriptive realism and as a contrast to S2.

\paragraph{S2 --- random stragglers.}
Each communication round, $\lfloor 0.5 \times 7 \rfloor + 1 = 4$
clients are randomly designated stragglers (sampled without
replacement). Each straggler performs $E_i \sim
\mathrm{Uniform}\{1, 2, \dots, 19\}$ local epochs; the
remaining three clients perform the full $E = 20$. Aggregated
across the ten paired seeds, every client serves as a
straggler in approximately $57\%$ of rounds. The schedule
heatmap (Figure~\ref{fig:sh-schedules}, right) shows the
characteristic salt-and-pepper pattern of randomised straggler
assignment, with mean per-cell value of approximately $14.1$
epochs (mixture of full-work cells at $E = 20$ and partial-work
cells at $E_i \in \{1, \dots, 19\}$). S2 follows the protocol
of Li et al.\ (2020), §5.2 up to two implementation details:
stragglers always perform at least one local optimisation step
(the Li et al.\ protocol allows $E_i = 0$), and both algorithms
include partial updates in aggregation. We replicate the
canonical Li et al.\ asymmetric setup (FedAvg drops stragglers
while FedProx includes them) separately in
Section~\ref{sec:sh-asymmetric}.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_sh_schedules.pdf}
\caption{Per-(round, client) local-epoch schedules under the
three system-heterogeneity conditions, reconstructed
deterministically from seed~$42$. Each heatmap row is a
communication round (rounds $1$--$150$, bottom to top) and
each column is one of the seven clients; cell colour shows the
local-epoch budget $E$ assigned to that (round, client) pair
on a $0$--$20$ scale. \textbf{Left (S0):} every client performs
the maximum $E = 20$ in every round (uniform yellow).
\textbf{Centre (S1):} clients C5 and C6 always perform
$E = 5$ (solid dark band on the two rightmost columns); the
remaining five clients always perform $E = 20$.
\textbf{Right (S2):} four of seven clients per round are
randomly designated stragglers with $E_i$ uniformly drawn from
$\{1, \dots, 19\}$; the remaining three clients perform the
full $E = 20$.}
\label{fig:sh-schedules}
\end{figure}

\paragraph{Training setup (held constant across conditions).}
All hyperparameters other than the schedule are held identical
to the engineered-partition headline of
Section~\ref{sec:engineered-headline}: same
\texttt{DermMNISTCNN} architecture, SGD with learning rate
$0.01$, momentum $0.9$, batch size $32$, weight decay $0$,
$R = 150$ communication rounds, full participation $C = 1.0$,
FedProx proximal coefficient $\mu = 0.01$, and the same ten
paired seeds. The Flower simulation runtime is used throughout
this chapter so that all S0/S1/S2 results share a single
runtime and the within-condition $\Delta$ does not mix
algorithmic effect with cross-runtime variation.

% ==============================================================================
\section{Results on the Engineered Partition}
\label{sec:sh-engineered-results}
% ==============================================================================

This section reports the FedAvg-vs-FedProx paired comparison
under each of the three computational-work heterogeneity
conditions on the engineered partition. The same ten paired seeds
are used across all conditions; within each seed, FedAvg and
FedProx share an identical local-epoch schedule, partition
assignment, and initial weights, so within-pair differences
reflect only the algorithmic gating of the proximal term. The
engineered partition is used as the primary testbed because it
is the setting in which statistical heterogeneity already
favours FedProx (Section~\ref{sec:engineered-headline}); to
separate the computational-work heterogeneity effect from the
statistical-heterogeneity baseline, Section~\ref{sec:sh-iid-isolation}
reruns the same S1 and S2 conditions on the IID partition.

\subsection{Results}
\label{sec:sh-engineered-table}

Table~\ref{tab:sh-engineered} reports test macro-F1 at the
best-validation checkpoint across the ten paired seeds under
each condition. Figure~\ref{fig:sh-engineered-convergence}
shows the corresponding per-round validation macro-F1
trajectories.

\begin{table}[!htbp]
\centering
\caption{System-heterogeneity results on the engineered
partition (Flower runtime, $n = 10$ paired seeds). Test
macro-F1 reported as mean $\pm$ standard deviation across
seeds; the winning algorithm per row is shown in bold. The
$\Delta$ column is the mean within-pair difference (FedProx
$-$ FedAvg). The S0 baseline row reproduces the engineered-
partition Flower result from
Section~\ref{sec:engineered-headline} for direct comparison.}
\label{tab:sh-engineered}
\small
\begin{tabular}{lccccc}
\toprule
Condition & FedAvg & FedProx & $\Delta$ & Wins \\
\midrule
S0 (no comp-work het)                & $0.4964 \pm 0.0150$ & $\mathbf{0.5033 \pm 0.0206}$ & $+0.007$ & $7/10$ \\
S1 (fixed stragglers, C5+C6 @ E=5) & $0.4978 \pm 0.0163$ & $\mathbf{0.5111 \pm 0.0196}$ & $+0.013$ & $8/10$ \\
S2 (random stragglers, 4/7)        & $0.4928 \pm 0.0277$ & $\mathbf{0.5102 \pm 0.0168}$ & $+0.017$ & $7/10$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_sh_engineered_convergence.pdf}
\caption{Validation macro-F1 over $150$ communication rounds
on the engineered \texttt{balanced\_paired\_7\_clients}
partition under each of the three system-heterogeneity
conditions (Flower runtime, $n = 10$ paired seeds, mean $\pm$
SEM band). The FedProx curve (dark slate blue) maintains a
visible upward separation from the FedAvg curve (mint green)
in all three conditions, with the gap widening from S0 (left)
to S2 (right).}
\label{fig:sh-engineered-convergence}
\end{figure}

\subsection{Interpretation}
\label{sec:sh-engineered-interpretation}

The within-pair $\Delta$ rises monotonically with straggler
severity across the three conditions: $+0.007$ macro-F1 at the
baseline S0, $+0.013$ at S1 with two consistently slow clients,
and $+0.017$ at S2 with four randomly designated stragglers
per round. FedProx wins the majority of paired seeds in each
condition ($7/10$, $8/10$, $7/10$). The convergence figure
shows this pattern visually: the FedAvg-vs-FedProx gap is
narrowest under S0 and visibly wider under S1 and S2,
particularly in the late-training plateau.

The monotonic increase aligns with the canonical motivation
of Li et al.\ (2020) for the proximal regulariser. As more
local computation per client becomes inexact ($E_i < E_{\max}$
on a growing fraction of (round, client) cells across the
three conditions), the parameter-update trajectories of the
participating clients diverge more from the broadcast global
anchor $w^t$. The proximal anchor's drift-control role is
therefore expected to contribute more under increasing
straggler severity, and the empirical monotonic pattern in
Table~\ref{tab:sh-engineered} is consistent with this
mechanism.

The absolute magnitudes are small in macro-F1 units
($0.007$--$0.017$), comparable in size to the across-seed
standard deviation of either algorithm under each condition
($\pm 0.015$--$\pm 0.028$). We therefore report the
condition-level $\Delta$ values descriptively, as a
qualitatively consistent direction-of-effect pattern rather
than as quantitatively distinguishable effect sizes between
adjacent conditions. The direction-of-effect pattern (positive
under non-IID stragglers, monotonic with severity) is the
substantive observation.

The S1 condition's positive $\Delta$ should be read together
with the partition-design caveat noted in
Section~\ref{sec:system-het-design}: under the engineered
partition, the two designated S1 stragglers (C5 and C6) are
the melanoma/vascular specialist client and the nevi-only
generalist client respectively. The S1 result therefore
reflects the specific scenario in which a clinical specialist
client and the generalist client are both consistently slow.
The S2 condition rotates straggler identity randomly per
round and therefore does not have this confound; the $+0.017$
macro-F1 advantage under S2 is the cleaner direct test of the
system-heterogeneity effect in the engineered-partition
configuration.

\subsection{Connection to the parameter-level evidence}
\label{sec:sh-engineered-connection}

The outcome-level pattern reported in
Table~\ref{tab:sh-engineered} is consistent with the
parameter-level evidence of Section~\ref{sec:update-norms}: the
per-client update-norm ratio $\lVert \Delta w \rVert_{\mathrm{FedAvg}} /
\lVert \Delta w \rVert_{\mathrm{FedProx}}$ on the engineered
partition was $1.48\times$ at the S0 baseline, and the
$\gamma$-inexact regime introduced by S1 and S2 increases the
client-side drift on which the proximal regulariser acts
(Li et al.\ 2020, Theorem~4). The larger within-pair $\Delta$
values observed under S1 and S2 are consistent with this role.
Whether the increment is attributable to system heterogeneity
\emph{per se} or requires statistical heterogeneity to be
present is examined in Section~\ref{sec:sh-iid-isolation}.

\subsection{Isolation: system heterogeneity on the IID partition}
\label{sec:sh-iid-isolation}

The engineered-partition results above hold statistical
heterogeneity present in every row, so the within-pair
$\Delta$ at S1 and S2 conflates two effects: the
statistical-heterogeneity baseline that already favours FedProx
on the engineered partition, and the additional contribution
from variable local-epoch budgets. To isolate the
pure computational-work heterogeneity effect, the same S1 and S2
conditions were rerun on the IID partition, where the
no-drift baseline (S0~IID) gives $\Delta \approx 0$
(Section~\ref{sec:iid-baseline}). All other hyperparameters
match the engineered-partition runs and the same ten paired
seeds are used; the comparison is summarised in
Table~\ref{tab:sh-iid-isolation}.

\begin{table}[!htbp]
\centering
\caption{System-heterogeneity conditions on the IID partition
(Flower runtime, $n = 10$ paired seeds). Mean test macro-F1 is
reported with standard deviation across seeds; $\Delta$ is the
mean within-pair difference (FedProx $-$ FedAvg); the $95\%$ CI
is the distribution-free Walsh interval on the median $\Delta$
and $p$ is the exact two-sided Wilcoxon signed-rank test.}
\label{tab:sh-iid-isolation}
\small
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{@{}l c c r c r@{}}
\toprule
Condition (IID partition)             & FedAvg              & FedProx             & $\Delta$  & $95\%$ CI         & $p$    \\
\midrule
S0 (no stragglers)                    & $0.5846 \pm 0.0197$ & $0.5786 \pm 0.0190$ & $-0.006$  & $[-0.021, +0.006]$ & $0.56$ \\
S1 (fixed stragglers, C5+C6 @ $E=5$)  & $0.5741 \pm 0.0188$ & $0.5843 \pm 0.0140$ & $+0.010$  & $[-0.010, +0.028]$ & $0.23$ \\
S2 (random stragglers, $4/7$)         & $0.5879 \pm 0.0112$ & $0.5747 \pm 0.0149$ & $-0.013$  & $[-0.026, -0.000]$ & $0.05$ \\
\bottomrule
\end{tabular}
\end{table}

The IID isolation produces a substantively different pattern
from the engineered partition. Under IID + S1 the within-pair
$\Delta$ is $+0.010$ but the CI includes zero
($p = 0.23$). Under IID + S2 --- the cleaner randomised
straggler test --- FedProx is on average \emph{worse} than
FedAvg by $-0.013$ macro-F1, with a CI that just excludes zero
($p = 0.05$) and FedProx winning only $3/10$ paired seeds.
The monotone S0~$\to$~S1~$\to$~S2 enlargement of the FedProx
advantage observed on the engineered partition therefore does
not appear on IID; on IID, the same straggler manipulation
produces no improvement under S1 and a small regression under
S2. The reading is that the $\Delta = +0.017$ reported under
the engineered S2 is not a pure system-heterogeneity effect:
the engineered partition's inter-client drift is necessary for
the proximal regulariser to act on the stragglers' inexact
updates. Without that drift, the regulariser shrinks legitimate
local progress and the outcome moves against FedProx. The
chapter's S1 and S2 results are therefore interpreted as
\emph{joint statistical-and-system-heterogeneity} effects rather
than as pure computational-work heterogeneity isolations.

% ==============================================================================
\section{Per-Class Breakdown under S2}
\label{sec:sh-c2-per-class}
% ==============================================================================

The aggregate macro-F1 result reported in
Section~\ref{sec:sh-engineered-results} averages over the seven
DermaMNIST classes and does not reveal which classes contribute
to the $+0.017$ within-pair $\Delta$ under the S2 random-
straggler condition. This section decomposes the S2 result by
class, mirroring the per-class breakdown performed for the
no-system-het engineered baseline in
Section~\ref{sec:engineered-per-class}. The expected pattern,
based on the partition's structural design, is that classes
held by exactly two clients should show a positive $\Delta$
(inter-client drift exists for the proximal anchor to act on)
while the majority class melanocytic nevi (held by every
client) should show $\Delta \approx 0$.

\subsection{Setup}
\label{sec:sh-c2-per-class-setup}

The data are the same S2 paired runs analysed in
Section~\ref{sec:sh-engineered-results}: ten paired seeds on
the engineered \texttt{balanced\_paired\_7\_clients} partition
with the random-straggler schedule, $\mu = 0.01$ for FedProx
and $\mu = 0$ for FedAvg, all other hyperparameters identical
to the S0 baseline. Per-class F1 scores are computed on the
global test set ($n = 2{,}005$ samples) at each run's best-
validation checkpoint.

\subsection{Results}
\label{sec:sh-c2-per-class-results}

Table~\ref{tab:sh-c2-per-class} reports per-class test F1
across the ten paired seeds. Figure~\ref{fig:sh-c2-per-class}
shows the corresponding per-class validation F1 trajectories.

\begin{table}[!htbp]
\centering
\caption{Per-class test F1 under the S2 random-straggler
condition on the engineered partition (Flower runtime, $n = 10$
paired seeds, mean $\pm$ standard deviation across seeds).
The winning algorithm per row is shown in bold; the $\Delta$
column is the mean within-pair difference (FedProx $-$ FedAvg).
Wins are the count of paired seeds in which FedProx outperforms
FedAvg on that class.}
\label{tab:sh-c2-per-class}
\small
\begin{tabular}{lcccc}
\toprule
Class (prevalence) & FedAvg & FedProx & $\Delta$ & Wins \\
\midrule
Actinic keratoses (3.27\%)        & $0.454 \pm 0.060$ & $\mathbf{0.481 \pm 0.038}$ & $+0.026$ & $7/10$ \\
Basal cell carcinoma (5.13\%)     & $0.492 \pm 0.057$ & $\mathbf{0.497 \pm 0.023}$ & $+0.005$ & $4/10$ \\
Benign keratosis (10.97\%)        & $0.428 \pm 0.066$ & $\mathbf{0.432 \pm 0.045}$ & $+0.003$ & $4/10$ \\
Dermatofibroma (1.15\%)           & $0.255 \pm 0.065$ & $\mathbf{0.288 \pm 0.103}$ & $+0.034$ & $6/10$ \\
Melanoma (11.11\%)                & $0.233 \pm 0.065$ & $\mathbf{0.318 \pm 0.058}$ & $\mathbf{+0.085}$ & $\mathbf{10/10}$ \\
Melanocytic nevi (67.05\%)        & $0.884 \pm 0.005$ & $\mathbf{0.886 \pm 0.004}$ & $+0.001$ & $7/10$ \\
Vascular lesions (1.41\%)         & $\mathbf{0.703 \pm 0.066}$ & $0.670 \pm 0.039$ & $-0.033$ & $4/10$ \\
\bottomrule
\end{tabular}
\end{table}

The per-class decomposition under S2 closely mirrors the
per-class structure observed in the no-system-het baseline of
Section~\ref{sec:engineered-per-class}. Melanoma stands out as
the dominant contributor: FedProx improves melanoma F1 by
$+0.085$ on average and wins on \emph{every} one of the ten
paired seeds. The actinic keratoses class shows the next-
largest positive difference ($\Delta = +0.026$, $7/10$ wins),
followed by dermatofibroma ($\Delta = +0.034$, $6/10$ wins).
The majority class melanocytic nevi again produces a
near-zero $\Delta$ ($+0.001$ macro-F1, both algorithms within
$\pm 0.005$ across seeds) --- the expected signature of the
proximal-anchor mechanism on a class held by every client.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_sh_c2_per_class.pdf}
\caption{Validation F1 over 150 communication rounds for each
DermaMNIST class under the S2 random-straggler condition on the
engineered partition. Curves show mean validation F1 across 10
paired seeds with $\pm$SEM bands. Final test-set $\Delta$ values
at the best-validation checkpoint are reported separately in
Table~\ref{tab:sh-c2-per-class}.}
\label{fig:sh-c2-per-class}
\end{figure}

\subsection{Comparison with the S0 baseline}
\label{sec:sh-c2-per-class-vs-c0}

The S0 (no comp-work het) baseline reported in
Section~\ref{sec:engineered-per-class} showed a melanoma
$\Delta$ of $+0.114$ on the pure-PyTorch primary; under the
Flower runtime which is used in this chapter the corresponding
S0 melanoma $\Delta$ is $+0.068$ ($7/10$ wins). The S2
random-straggler condition produces a melanoma $\Delta$ of
$+0.085$ ($10/10$ wins) --- larger in mean magnitude than the
Flower S0 baseline and unanimous in direction across seeds.
The melanocytic-nevi $\Delta$ remains near zero in both
conditions ($-0.002$ at S0 Flower, $+0.001$ at S2). The
per-class pattern that the macro-F1 advantage is concentrated
on the melanoma class and absent on the universal melanocytic
nevi class is preserved under system heterogeneity, with the
melanoma signal becoming slightly stronger and more
seed-consistent under S2 than under S0.

% ==============================================================================
\section{Replication of Li et al.\ (2020) §5.2 Asymmetric Protocol}
\label{sec:sh-asymmetric}
% ==============================================================================

\paragraph{Status of this experiment.}
This experiment is included as a \emph{protocol replication} of
Li et al.\ (2020), §5.2, not as an isolated estimate of the
proximal term's effect. The asymmetric setup changes two things at
once relative to the rest of this chapter: FedAvg drops straggler
updates, and FedProx both includes their partial updates and uses
the proximal term. The resulting $\Delta$ therefore conflates the
inclusion/exclusion policy with the proximal regulariser and is
not directly comparable to the symmetric S2 $\Delta = +0.017$
reported in Section~\ref{sec:sh-engineered-results}, which is the
clean algorithmic isolation. The clean algorithmic isolation
remains the load-bearing system-heterogeneity result of this
chapter.

The S0/S1/S2 results reported in
Sections~\ref{sec:sh-engineered-results}--\ref{sec:sh-c2-per-class}
use a \emph{symmetric} inclusion policy: both FedAvg and FedProx
aggregate over all clients including stragglers, weighted by
dataset size. The canonical straggler protocol of
Li et al.\ (2020), §5.2 is asymmetric: FedAvg \emph{drops}
straggler updates (clients that do not complete the full
$E_{\max}$ local epochs are excluded from aggregation), while
FedProx \emph{includes} their partial updates as
$\gamma$-inexact contributions. To verify that the canonical
asymmetric setup reproduces qualitatively on DermaMNIST, we
additionally run the FedAvg-drop arm on the engineered
partition under the S2 random-straggler schedule. Combined with
the existing FedProx-include arm from the symmetric S2 data of
Section~\ref{sec:sh-engineered-results} on the same ten seeds,
this gives the paired comparison in
Table~\ref{tab:sh-asymmetric}.

\begin{table}[!htbp]
\centering
\caption{Replication of the Li et al.\ (2020) §5.2 asymmetric
straggler protocol on the engineered partition with S2 random
stragglers (Flower runtime, $n = 10$ paired seeds). Test
macro-F1 reported as mean $\pm$ standard deviation across
seeds; the winning configuration is shown in bold.}
\label{tab:sh-asymmetric}
\small
\begin{tabular}{lccc}
\toprule
Configuration & Test macro-F1 & $\Delta$ & Wins \\
\midrule
FedAvg (drops straggler updates)         & $0.4423 \pm 0.0192$ & ---       & ---           \\
FedProx (includes straggler updates)     & $\mathbf{0.5102 \pm 0.0168}$ & $\mathbf{+0.068}$ & $\mathbf{10/10}$ \\
\bottomrule
\end{tabular}
\end{table}

The FedProx-include configuration outperforms FedAvg-drop by a
mean within-pair difference of $+0.068$ macro-F1, with FedProx
winning all $10$ paired seeds. This reproduces the qualitative
finding of Li et al.\ (2020), §5.2 on the DermaMNIST task: the
canonical asymmetric straggler setup produces a substantially
higher macro-F1 under FedProx than under FedAvg. The magnitude
is approximately four times larger than the symmetric S2
$\Delta$ of $+0.017$ reported in
Section~\ref{sec:sh-engineered-results}, primarily because the
asymmetric setup makes two simultaneous changes to FedAvg
relative to FedProx (dropping partial updates and lacking the
proximal regulariser) rather than only one.

\subsection{Validation-trajectory decomposition}
\label{sec:sh-asymmetric-trajectories}

Figure~\ref{fig:val-curves-extreme-gaps} resolves the four-condition
decomposition over training time and extends it to the more extreme
perfect-storm regime of Section~\ref{sec:perfect-storm}. The
trajectories make the underlying mechanism legible at a glance.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_val_curves_extreme_gaps.pdf}
\caption{Validation macro-F1 trajectories at the two experiments
showing the largest FedAvg vs FedProx differences. Curves are mean
over three seeds ($42, 123, 456$); shaded bands are $\pm 1$ sample
SD. \textbf{(A)} Li 2020 §5.2 four-condition decomposition. The
FedAvg-no-straggler and FedProx-with-$\gamma$-inexact curves both
climb to $\sim 0.45$, while FedAvg+drop and FedProx+drop (control)
remain stuck at $\sim 0.36$ and overlap nearly perfectly across all
$150$ rounds --- the proximal term in isolation contributes nothing
under matched drop-stragglers handling. \textbf{(B)} Perfect-storm
L4 with $90\%$ random stragglers per round. FedAvg+drop flatlines at
$0.087$ for the entire training run (no recovery); FedProx with
$\mu = 1.0$ + $\gamma$-inexact climbs gradually to $\sim 0.33$;
FedProx with $\mu = 0.01$ + $\gamma$-inexact climbs to $\sim 0.47$.}
\label{fig:val-curves-extreme-gaps}
\end{figure}

Three features of the trajectories are worth emphasising. First, in
Panel A the FedAvg-no-straggler and FedProx-$\gamma$-inexact curves
are nearly indistinguishable: under FedProx's partial-update
handling, an asymmetric straggler protocol is reduced to
approximately the no-straggler baseline. Second, the FedAvg+drop
and FedProx+drop (control) curves in Panel A overlap throughout
training, visualising the $+0.004$ proximal-only effect of the
four-condition decomposition: the proximal regulariser by itself
does not recover the dropped information. Third, in Panel B the
FedAvg curve is genuinely flat across all $150$ rounds at $\sim
0.087$ macro-F1 --- the model never learns under $90\%$ stragglers
with drop-handling, because in most rounds only one client
contributes to the aggregation and the global model collapses to
predicting only that client's local distribution.

% ==============================================================================
\section{Partial Client Participation}
\label{sec:sh-partial-participation}
% ==============================================================================

In addition to the local-epoch axis tested under S1 and S2, the
literature varies the per-round participation fraction $C$
(McMahan et al.\ 2017; Li et al.\ 2020, §5.3; Kairouz et al.\ 2021).
This section reports the FedAvg-vs-FedProx comparison under
partial client participation at $C = 0.5$ (four of seven clients
sampled uniformly at random per round) on the engineered partition,
with all other hyperparameters held identical to the S0 engineered
baseline. Theory predicts the proximal anchor is most useful when
the per-round aggregation pool is smaller (more drift to constrain
per included client).

The two algorithm arms share the same starting seed but on $5$ of
the $10$ seeds the per-round sampled client sets desync between
arms (FedProx consumes one additional RNG draw per round). The two
arms at $C = 0.5$ are therefore not strictly paired; outcome-level
results are reported as arm summaries and the load-bearing claim
is the within-arm update-norm comparison
(Section~\ref{sec:sh-partial-update-norms}), which is unaffected
by the sampling realisation.

\subsection{Results}
\label{sec:sh-partial-results}

Table~\ref{tab:sh-partial} reports the test macro-F1 at the
best-validation checkpoint under partial participation, side by
side with the full-participation engineered S0 baseline from
Section~\ref{sec:sh-engineered-results}.
Figure~\ref{fig:sh-partial-convergence} shows the corresponding
per-round validation macro-F1 trajectories.

\begin{table}[!htbp]
\centering
\caption{Partial-participation result on the engineered partition
($n = 10$ seeds per arm, Flower runtime). The first row
reproduces the full-participation baseline from
Section~\ref{sec:sh-engineered-results}; the second row reports
the partial-participation result. Test macro-F1 is reported as
mean $\pm$ standard deviation across seeds per arm. The
$\Delta$ column is the difference of arm means, reported
descriptively (see the setup note for why a paired-comparison
interpretation is not claimed here). The higher arm mean per
row is shown in bold.}
\label{tab:sh-partial}
\small
\begin{tabular}{lccc}
\toprule
Setting & FedAvg & FedProx & $\Delta$ (mean) \\
\midrule
$C = 1.0$ (full participation)         & $0.4964 \pm 0.0150$ & $\mathbf{0.5033 \pm 0.0206}$ & $+0.007$ \\
$C = 0.5$ (partial, 4/7 per round)     & $0.4780 \pm 0.0234$ & $\mathbf{0.4944 \pm 0.0185}$ & $+0.016$ \\
\bottomrule
\end{tabular}
\end{table}

The arm gap doubles from $+0.007$ at $C = 1.0$ to $+0.016$ at
$C = 0.5$. The across-seed SDs are of the same order as the gap,
and the arms are not strictly paired (see Setup), so the
outcome-level comparison is supportive rather than load-bearing.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_partial_participation.pdf}
\caption{Validation macro-F1 over $150$ communication rounds on
the engineered partition under full and partial client
participation (Flower runtime, $n = 10$ seeds per arm, mean
$\pm$ SEM band). \textbf{Left ($C = 1.0$):} all seven clients
participate every round; FedProx maintains a modest separation
from FedAvg. \textbf{Right ($C = 0.5$):} four of seven clients
are sampled uniformly at random per round; the FedProx
validation curve sits above the FedAvg curve throughout late
training, and the gap between the two arm means is visibly
wider than under full participation. Final test-set arm means
are reported in Table~\ref{tab:sh-partial}.}
\label{fig:sh-partial-convergence}
\end{figure}

FedAvg loses $0.018$ macro-F1 in absolute terms going from
$C = 1.0$ to $C = 0.5$ ($0.496 \to 0.478$); FedProx loses
$0.009$ ($0.503 \to 0.494$). The drop sizes are comparable to
the across-seed SDs, so this is reported descriptively.

\subsection{Per-class breakdown}
\label{sec:sh-partial-per-class}

Table~\ref{tab:sh-partial-per-class} decomposes the
partial-participation arm comparison by class. The per-class
pattern matches the structure observed under both the engineered
S0 baseline and the S2 random-straggler condition: melanoma is
the dominant contributor to the macro-F1 arm gap, and the
universal class melanocytic nevi is mechanistically inert.

\begin{table}[!htbp]
\centering
\caption{Per-class test F1 under partial participation $C = 0.5$
on the engineered partition (Flower runtime, $n = 10$ seeds per
arm, mean $\pm$ standard deviation across seeds per arm). The
higher arm mean per row is shown in bold; $\Delta$ is the
difference of arm means (FedProx $-$ FedAvg) reported
descriptively.}
\label{tab:sh-partial-per-class}
\small
\begin{tabular}{lccc}
\toprule
Class (prevalence) & FedAvg & FedProx & $\Delta$ (mean) \\
\midrule
Actinic keratoses (3.27\%)        & $0.453 \pm 0.040$ & $\mathbf{0.462 \pm 0.044}$ & $+0.008$ \\
Basal cell carcinoma (5.13\%)     & $0.454 \pm 0.045$ & $\mathbf{0.472 \pm 0.040}$ & $+0.018$ \\
Benign keratosis (10.97\%)        & $0.403 \pm 0.069$ & $\mathbf{0.410 \pm 0.052}$ & $+0.006$ \\
Dermatofibroma (1.15\%)           & $0.238 \pm 0.093$ & $\mathbf{0.277 \pm 0.077}$ & $+0.039$ \\
Melanoma (11.11\%)                & $0.199 \pm 0.063$ & $\mathbf{0.282 \pm 0.033}$ & $\mathbf{+0.084}$ \\
Melanocytic nevi (67.05\%)        & $\mathbf{0.881 \pm 0.007}$ & $0.882 \pm 0.004$ & $+0.002$ \\
Vascular lesions (1.41\%)         & $\mathbf{0.718 \pm 0.036}$ & $0.675 \pm 0.052$ & $-0.043$ \\
\bottomrule
\end{tabular}
\end{table}

Melanoma drives the arm gap ($\Delta = +0.084$, comparable to
$+0.085$ under S2 and larger than the $+0.068$ S0 baseline) and
melanocytic nevi remains at $\Delta \approx +0.002$. The single
regression is \emph{vascular lesions} ($\Delta = -0.043$): held by
clients C4 and C5 only, so at $C = 0.5$ both are sampled out
together in $\binom{5}{4}/\binom{7}{4} \approx 14\%$ of rounds,
and the $n = 99$ vascular training samples globally leave little
capacity to recover. This is a deployment-relevant caveat for rare
classes concentrated on a small number of clients.

\subsection{Mechanism evidence: update norms}
\label{sec:sh-partial-update-norms}

The per-client update-norm comparison is a within-arm scalar and
is unaffected by which specific clients were sampled per round, so
it is the load-bearing claim of this section.
Table~\ref{tab:sh-partial-norms} reports the mean per-client
update norm $\lVert w_i^{t+1} - w^t \rVert_2$ under each
participation regime.

\begin{table}[!htbp]
\centering
\caption{Per-client update norm averaged across all (round,
client) pairs and across paired seeds, on the engineered
partition (Flower runtime). The ratio column reports the FedAvg
mean update norm divided by the FedProx mean update norm under
each participation regime.}
\label{tab:sh-partial-norms}
\small
\begin{tabular}{lccc}
\toprule
Setting & FedAvg $\lVert \Delta w \rVert$ & FedProx $\lVert \Delta w \rVert$ & Ratio FA/FP \\
\midrule
$C = 1.0$ (full participation)    & $1.523$ & $\mathbf{1.029}$ & $1.48\times$ \\
$C = 0.5$ (partial participation) & $1.669$ & $\mathbf{1.078}$ & $\mathbf{1.55\times}$ \\
\bottomrule
\end{tabular}
\end{table}

Both algorithms produce larger update norms under partial
participation than under full ($1.52 \to 1.67$, $1.03 \to 1.08$).
The FedAvg-to-FedProx ratio enlarges from $1.48\times$ to
$1.55\times$: the regulariser is empirically more active in the
regime where theory said it should be most useful.


% ==============================================================================
% High-priority results: multi-seed promotion + cross-partition mu sweep +
% perfect-storm L4 + cross-protocol mechanism map.
% ==============================================================================

\section{Multi-Seed Replication at L4: Node-Pinned}
\label{sec:node-pinned-l4}

The primary engineered-partition result of
Section~\ref{sec:engineered-headline} rests on a single seed.
To distinguish a possible algorithmic effect from cross-seed and
cross-node CUDA non-determinism noise, we replicated the L4
comparison with three seeds, pinning all jobs to a single physical
compute node to remove cross-node variability as a confounder.

\subsection{Setup}
Three seeds ($42, 123, 456$) $\times$ two algorithms (FedAvg and
FedProx with $\mu = 0.01$) on the engineered L4 partition
(\texttt{two\_client\_90\_10\_rare\_stress}), 150 rounds, symmetric
protocol, all SLURM jobs pinned to the same physical compute node.

\subsection{Results}
Table~\ref{tab:node-pinned-l4} reports test macro-F1.

\begin{table}[!htbp]
\centering
\caption{Node-pinned multi-seed test macro-F1 at L4 under the
symmetric protocol (mean $\pm$ sample SD over three seeds).}
\label{tab:node-pinned-l4}
\small
\begin{tabular}{lc}
\toprule
Algorithm & macro-F1 \\
\midrule
FedAvg                  & $0.492 \pm 0.019$ \\
FedProx ($\mu = 0.01$)  & $0.486 \pm 0.019$ \\
\bottomrule
\end{tabular}
\end{table}

The mean difference is $\Delta = 0.006$ macro-F1, approximately
one-sixth of the pooled cross-seed standard deviation
($\sigma = 0.019$) and well below the conservative threshold of two
pooled standard deviations ($0.038$) we use to flag a potentially
meaningful effect. We therefore conclude that, under the symmetric
protocol at L4, FedAvg and FedProx are not meaningfully separated
within the observed seed-level variation. The original single-seed
pattern reported in Section~\ref{sec:engineered-headline}
(FedProx approximately $0.026$ below FedAvg) is consistent with
seed-level noise rather than a reproducible algorithmic disadvantage.

\paragraph{Reporting-protocol sensitivity.} The same three seeds
yield different FedAvg-vs-FedProx orderings depending on whether
test macro-F1 is read at the best-validation checkpoint, at the
final round, or as a mean over the last $K = 25$ rounds: two of the
three seeds flip ordering across these three reporting protocols
(reporting-protocol flip rate $= 0.67$; see
Section~\ref{sec:robustness-checks}). This strengthens rather than
weakens the null conclusion: when the mean difference is comparable
to seed-level noise, the ordering between algorithms becomes
unstable to small reporting-protocol changes, exactly as expected
of a non-effect.

% ------------------------------------------------------------------

\section{Convergence-Budget Sensitivity at L3}
\label{sec:extended-rounds-l3}

The single-seed Stage A heterogeneity-ladder pilot of
Section~\ref{sec:heterogeneity-ladder} reports a small FedProx
deficit at L3 ($\Delta \approx -0.003$ macro-F1). The pilot also
recorded that FedProx's best-validation round at L3 was 150 --- the
final round of training --- suggesting the algorithm may not have
converged within the available budget. To test this convergence-budget
sensitivity we re-ran L3 with three seeds and an extended 250-round
budget.

\subsection{Results}
At 250 rounds, FedAvg achieves macro-F1 of $0.522 \pm 0.006$ and
FedProx $0.549 \pm 0.025$ (3 seeds, ddof $= 1$), a difference of
$+0.027$ in FedProx's favour. The mean best-validation round is
169 for FedAvg and 210 for FedProx, with FedProx's maximum reaching
238 --- close to the 250-round cap. We interpret this as evidence
that \textbf{neither algorithm has fully converged at 250 rounds at
L3}: the small differences observed at intermediate heterogeneity
are compute-budget-sensitive and should not be interpreted as a
robust algorithmic effect. The L3 comparison does not on its own
reverse the conclusion of Section~\ref{sec:node-pinned-l4}; we report
it here as a methodological caveat for the heterogeneity-ladder pilot.

% ------------------------------------------------------------------

\section{Cross-Partition $\mu$-Sensitivity}
\label{sec:mu-cross-partition}

The $\mu$-sensitivity analysis of Section~\ref{sec:mu-sensitivity}
is performed on the engineered partition. To assess whether the
optimal $\mu$ shifts with heterogeneity, we ran a $\mu$-sweep on
three ladder partitions (L0, L2, L4) at $\mu \in \{0.001, 0.01,
0.1, 1.0\}$, single seed.

\subsection{Results}
Table~\ref{tab:mu-cross-partition} reports test macro-F1.

\begin{table}[!htbp]
\centering
\caption{Test macro-F1 across $\mu$ and ladder partition (single-seed
Stage A pilot). FedAvg baseline shown for reference. Best per-row
$\mu$ in bold.}
\label{tab:mu-cross-partition}
\small
\begin{tabular}{lcccccc}
\toprule
Partition & JS  & FedAvg & $\mu{=}0.001$ & $\mu{=}0.01$ & $\mu{=}0.1$ & $\mu{=}1.0$ \\
\midrule
L0 (IID)        & $0.000$ & $0.608$ & $\mathbf{0.619}$ & $0.598$ & $0.598$ & $0.544$ \\
L2 (label skew) & $0.104$ & $0.549$ & $\mathbf{0.570}$ & $0.566$ & $0.532$ & $0.488$ \\
L4 (severe)     & $0.385$ & $\mathbf{0.518}$ & $0.461$ & $0.493$ & $0.427$ & $0.366$ \\
\bottomrule
\end{tabular}
\end{table}

Three patterns emerge. First, the optimal $\mu$ on this benchmark is
small at every partition: $\mu = 0.001$ at L0 and L2, and
$\mu = 0.01$ at L4 (although at L4 FedAvg still outperforms FedProx
at every $\mu$ tested). Second, $\mu = 1.0$ produces the worst
FedProx result at every partition, with the deficit growing
substantially with heterogeneity ($0.064$ below the best at L0,
$0.082$ at L2, $0.127$ at L4). Third, the pattern does not support,
in this benchmark, the practical prescription that larger $\mu$ be
preferred under stronger heterogeneity. We do not claim this refutes
the theoretical analysis of \citet{li2020fedprox}, which holds under
its stated assumptions; we report it as a pattern observation in the
specific regime tested.

% ------------------------------------------------------------------

\section{Perfect-Storm L4: Literature-Canonical Recipe}
\label{sec:perfect-storm}

The Li 2020 §5.2 four-condition decomposition of
Section~\ref{sec:sh-asymmetric} uses the thesis-default
hyperparameters. To check that the qualitative ordering of that
decomposition holds under the literature-canonical recipe
\citep{li2020fedprox}, we ran a more extreme variant: $90\%$ random
stragglers, three seeds, three conditions on L4.

\subsection{Setup}
Three (algorithm, condition) pairs --- FedAvg with drop-stragglers,
FedProx $\mu = 1.0$ with $\gamma$-inexact, FedProx $\mu = 0.01$ with
$\gamma$-inexact --- three seeds, $90\%$ random straggler rate per
round, batch size 10, no momentum (Li 2020 conventions).

\subsection{Results}
Table~\ref{tab:perfect-storm} reports test macro-F1.

\begin{table}[!htbp]
\centering
\caption{Perfect-storm L4 macro-F1 (3 seeds; mean $\pm$ sample SD).}
\label{tab:perfect-storm}
\small
\begin{tabular}{lc}
\toprule
Condition & macro-F1 \\
\midrule
FedAvg + drop-stragglers                  & $0.087 \pm 0.049$ \\
FedProx $\mu{=}1.0$ + $\gamma$-inexact    & $0.365 \pm 0.023$ \\
FedProx $\mu{=}0.01$ + $\gamma$-inexact   & $\mathbf{0.491 \pm 0.003}$ \\
\bottomrule
\end{tabular}
\end{table}

FedProx with $\gamma$-inexact handling exceeds FedAvg with
drop-stragglers by $+0.278$ macro-F1 at $\mu = 1.0$ and $+0.404$ at
$\mu = 0.01$. The qualitative ordering matches the Li 2020 §5.2
four-condition result of Section~\ref{sec:sh-asymmetric}. Two
observations are worth noting. First, the magnitude of the gap is
substantially larger than the $+0.115$ reported in
Section~\ref{sec:sh-asymmetric}, consistent with the much more
extreme $90\%$ random-straggler regime tested here. Second,
$\mu = 0.01$ outperforms $\mu = 1.0$ by $+0.126$ macro-F1 under this
recipe, consistent with the cross-partition $\mu$-sensitivity
observation of Section~\ref{sec:mu-cross-partition}: the literature
prescription that larger $\mu$ should be preferred at greater
heterogeneity is not supported on this benchmark even under the most
heterogeneity-favourable hyperparameter recipe. The perfect-storm
condition is reported here as confirmation of the qualitative
ordering; we do not use it as the headline because the multiple
simultaneous hyperparameter changes confound a clean four-condition
decomposition.

\subsection{Heterogeneity-escalation synthesis}
\label{sec:perfect-storm-synthesis}

Figure~\ref{fig:heterogeneity-escalation} synthesises the FedAvg vs
FedProx comparison across five stress conditions of increasing
severity, drawing on the results of
Sections~\ref{sec:engineered-headline} (IID baseline),
\ref{sec:node-pinned-l4} (L4 symmetric),
\ref{sec:sh-asymmetric} (Li 2020 §5.2 straggler asymmetry), and the
present section (perfect-storm at two $\mu$ values).

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_heterogeneity_escalation.pdf}
\caption{Heterogeneity-escalation synthesis. (A) Test macro-F1 of
FedAvg vs FedProx across five conditions of increasing system stress
(left to right); error bars are sample SD across the available
seeds (10 for IID, 3 for L4 conditions). $\Delta$ above each pair is
the within-pair FedProx $-$ FedAvg macro-F1. (B) The within-pair
$\Delta$ alone, ordered identically. The pattern is monotonic: the
FedProx advantage is essentially zero under IID and under symmetric
non-IID, becomes large under straggler asymmetry, and grows further
under the literature-canonical $90\%$ random-straggler regime.
FedProx's $\gamma$-inexact partial-update handling, not its proximal
term in isolation, is the operative mechanism (see the Li 2020 §5.2
four-condition decomposition in Section~\ref{sec:sh-asymmetric}).}
\label{fig:heterogeneity-escalation}
\end{figure}

The pattern reads as a direct answer to the question
\emph{under what conditions does the FedProx-vs-FedAvg difference
become large?} The within-pair $\Delta$ is essentially zero under
both the IID baseline and the L4 symmetric protocol, despite L4
being a strongly non-IID partition (JS divergence $0.385$): on this
benchmark, \emph{statistical heterogeneity alone is not sufficient}.
The gap opens substantially once \emph{system asymmetry} is
introduced through the Li 2020 §5.2 straggler protocol
($\Delta = +0.115$), and grows by a further factor of $2.4{-}3.5$
under the more extreme perfect-storm recipe
($\Delta = +0.278$ at $\mu = 1.0$, $+0.404$ at $\mu = 0.01$). The
operative mechanism, as decomposed in Section~\ref{sec:sh-asymmetric},
is FedProx's $\gamma$-inexact handling of partial straggler updates;
the proximal term alone contributes only $+0.004$ macro-F1 under
matched straggler handling. Statistical heterogeneity sets the
stage; system-level asymmetry is what makes FedProx shine.

% ------------------------------------------------------------------

\section{Cross-Protocol Mechanism Map at L4}
\label{sec:cross-protocol-mechanism}

The per-client update-norm evidence of Section~\ref{sec:update-norms}
analyses a single experimental condition. To examine whether the
update-norm pattern \emph{predicts} the per-class outcome across the
experimental conditions of this chapter, we pooled 78 L4 runs spanning
eight (algorithm, protocol) settings and tested the correlation
between aggregate update-norm summary statistics and final rare-class
F1.

\subsection{Setup}
For each (algorithm, protocol, seed) cell at L4, we computed the mean
per-client update norm averaged over all 150 rounds, then correlated
each summary statistic against the matched final rare-class average F1
(78 runs total). Eight protocols are included: node-pinned symmetric,
engineered baseline, heterogeneity-ladder L4, Li 2020 §5.2 conditions,
perfect-storm conditions, asymmetric per-client $\mu$, asymmetric
per-client learning rate, and weighted CE.

\subsection{Results}
The cross-protocol relationship between per-client update norms and
rare-class F1 is not simple. Within any one protocol family (e.g.,
the asymmetric-learning-rate experiment), Client~1's mean update
norm relative to Client~0's correlates positively with rare-class F1
(Spearman $r = +0.80$ for FedAvg, $r = +0.87$ for FedProx; both
within asymmetric-LR alone). Across protocols \emph{together},
however, this within-protocol ordering does not extrapolate: the
Spearman correlation between the relative-influence ratio and
rare-class F1 across all 78 runs is essentially zero ($r = -0.04$).

The strongest cross-protocol predictor of rare-class F1 we observe is
Client~0's mean update-norm magnitude. Pooling all 78 runs, the
Spearman correlation between Client~0's average update norm and the
final rare-class average F1 is $r = +0.63$. The dominant-client
update-norm magnitude is therefore associated with better rare-class
preservation across the protocols tested here. We interpret this
cautiously and as exploratory: it is a cross-condition correlation on
a small set of protocols, with no controlled intervention isolating
the relationship. It is consistent with the working interpretation
that rare-class learning on this benchmark proceeds substantially
through shared feature representations whose evolution is paced by
the dominant client's update activity, but we do not present it as a
derivation.

% ==============================================================================
% FedNova under unequal-E + Robustness Checks (Priority 2 follow-up)
% ==============================================================================

\section{FedNova under Unequal Local Epochs}
\label{sec:fednova-unequal-e}

The defining design motivation for FedNova~\citep{wang2020fednova} is to
correct the objective inconsistency that arises when participating clients
perform different numbers of local steps before each aggregation: its
$\tau_i$-normalisation rescales each client's update by its number of
local steps before averaging. We tested this design intent directly by
comparing all three algorithms under matched equal-$E$ vs unequal-$E$
conditions at the two more difficult ladder partitions.

\subsection{Setup}
Single-seed pilot. Equal-$E$ regime: both clients perform $E = 20$ local
epochs per round (the thesis-default configuration of
Section~\ref{sec:methods-protocol}). Unequal-$E$ regime: Client~0 performs
$E_0 = 20$ local epochs; Client~1 performs $E_1 = 5$ local epochs (a $4{:}1$
asymmetry, of the same order as the Li 2020 §5.2 straggler protocol of
Section~\ref{sec:sh-asymmetric}). Two partitions tested: L3 (mixed
$70/30$) and L4 (severe $90/10$). Three algorithms per (partition,
regime) cell.

\subsection{Results}
Table~\ref{tab:fednova-unequal-e} reports test macro-F1 and the
within-algorithm drop from equal-$E$ to unequal-$E$.

\begin{table}[!htbp]
\centering
\caption{Test macro-F1 at L3 and L4 under equal-$E$ ($E_0 = E_1 = 20$)
versus unequal-$E$ ($E_0 = 20, E_1 = 5$); single-seed pilot.
$\Delta_E$ is the within-algorithm drop from equal-$E$ to
unequal-$E$.}
\label{tab:fednova-unequal-e}
\small
\begin{tabular}{llccc}
\toprule
Partition & Algorithm & Equal-$E$ & Unequal-$E$ & $\Delta_E$ \\
\midrule
L3 & FedAvg                  & $0.511$ & $0.508$ & $-0.003$ \\
L3 & FedProx ($\mu = 0.01$)  & $0.549$ & $0.557$ & $+0.007$ \\
L3 & \textbf{FedNova}        & $\mathbf{0.535}$ & $\mathbf{0.310}$ & $\mathbf{-0.224}$ \\
\midrule
L4 & FedAvg                  & $0.482$ & $0.424$ & $-0.058$ \\
L4 & FedProx ($\mu = 0.01$)  & $0.478$ & $0.452$ & $-0.026$ \\
L4 & \textbf{FedNova}        & $\mathbf{0.522}$ & $\mathbf{0.330}$ & $\mathbf{-0.192}$ \\
\bottomrule
\end{tabular}
\end{table}

Two patterns emerge. First, both FedAvg and FedProx degrade only modestly
under unequal-$E$: at L4, FedAvg drops by $0.058$ and FedProx by $0.026$
macro-F1, comparable to seed-level variation observed elsewhere in this
chapter. Second, \textbf{FedNova degrades much more substantially}:
$-0.224$ macro-F1 at L3 and $-0.192$ at L4, an order of magnitude larger
than the corresponding drops for the other two algorithms.

\subsection{Interpretation}
The observation runs counter to FedNova's stated design intent: the
$\tau_i$-normalisation is the mechanism by which FedNova is intended to
\emph{correct} for unequal local steps, and unequal-$E$ is the regime
the algorithm was designed to handle. On this benchmark and at the two
non-trivial ladder partitions tested, the empirical effect is the
opposite: FedNova is the algorithm \emph{most} sensitive to unequal-$E$.

A plausible mechanism, which we do not test directly here, is that under
high statistical heterogeneity (L3 and L4) the rare-class-holding
client's $\tau_i$-normalised update is multiplied by a large rescaling
factor that amplifies the noise in its small-batch gradients, producing
divergent rather than convergent aggregation. We report this only as a
plausibility argument; the Wang 2020 convergence analysis assumes
smoothness and bounded local-gradient variance that may not hold under
the partition design of L4.

\subsection{Scope}
This is a \emph{single-seed} pilot finding. The magnitudes are large
relative to seed-level variation in the rest of this chapter, but
multi-seed replication is required before extending the observation to
a general claim about FedNova. We report it here as a counter-intuitive
empirical observation, not as a refutation of Wang 2020's theoretical
analysis in general.

% ------------------------------------------------------------------

\section{Robustness and Methodological Checks}
\label{sec:robustness-checks}

This section consolidates three methodological audits that strengthen
or qualify the interpretation of the preceding findings.

\subsection{Reporting-protocol sensitivity}
\label{sec:robustness-protocol-flip}

For each multi-seed experiment in this chapter we computed the test
macro-F1 under three reporting protocols: (i) test at the best-validation
checkpoint round (the thesis default), (ii) test at the final round, and
(iii) test averaged over the last $K = 25$ rounds. For each (algorithm,
seed) pair we then checked whether the ordering between the two algorithms
flipped sign across the three reporting protocols.

Table~\ref{tab:protocol-flip} reports the flip rate per experiment.

\begin{table}[!htbp]
\centering
\caption{Reporting-protocol flip rates per experiment. ``flip rate''
is the fraction of seeds for which the algorithm ordering changes
sign across the three reporting protocols (best-val, final-round,
last-$K$ mean).}
\label{tab:protocol-flip}
\small
\begin{tabular}{lcc}
\toprule
Experiment & Mean $|\Delta|$ (best-val) & Flip rate \\
\midrule
Perfect-storm L4               & $0.293$ & $0.00$ \\
Li 2020 §5.2 asymmetric         & $0.138$ & $0.00$ \\
Asymmetric per-client $\mu$     & $0.043$ & $0.00$ \\
Engineered baseline (L4)        & $0.023$ & $0.33$ \\
Extended-rounds L3              & $0.011$ & $0.33$ \\
Node-pinned L4 symmetric        & $0.015$ & $0.67$ \\
\bottomrule
\end{tabular}
\end{table}

The pattern is consistent: the three large-effect experiments (mean
$|\Delta| \geq 0.04$) have flip rate $0.00$, and the three small-effect
experiments have flip rate $\geq 0.33$. Across all six experiments
($18$ seed-level pairwise comparisons in total), $4$ comparisons flip;
$0/9$ flip on large effects and $4/9$ flip on small effects. We use
this pattern to inform our reporting throughout this chapter: large
within-pair differences are reported as ordered effects, while
small-magnitude observations (such as the L4 symmetric null of
Section~\ref{sec:node-pinned-l4}) are reported with explicit
reporting-protocol disclosure rather than as numerically precise effect
sizes.

\subsection{Training stability across algorithms}
\label{sec:robustness-stability}

Following the framework of~\citet{charles2024stability}, we measured the
standard deviation of validation macro-F1 over the last $50$ rounds of
training for every run in this chapter. The pooled means by (algorithm,
regime) cell are reported in Table~\ref{tab:training-instability}.

\begin{table}[!htbp]
\centering
\caption{Training instability: standard deviation of validation macro-F1
over the last $50$ rounds, pooled across all runs in the chapter
(mean $\pm$ across-run sample SD; $n$ = number of pooled runs).}
\label{tab:training-instability}
\small
\begin{tabular}{llrl}
\toprule
Algorithm & Regime & $n$ & Instability \\
\midrule
FedAvg   & symmetric         & $22$ & $0.024 \pm 0.012$ \\
FedAvg   & unequal-$E$        & $2$  & $0.020 \pm 0.014$ \\
FedAvg   & drop-stragglers   & $6$  & $0.001 \pm 0.001$ \\
FedProx  & symmetric         & $37$ & $0.019 \pm 0.017$ \\
FedProx  & unequal-$E$        & $11$ & $0.028 \pm 0.009$ \\
FedProx  & drop-stragglers   & $3$  & $0.002 \pm 0.002$ \\
\textbf{FedNova} & symmetric         & $5$  & $\mathbf{0.048 \pm 0.039}$ \\
\textbf{FedNova} & unequal-$E$        & $2$  & $\mathbf{0.060 \pm 0.031}$ \\
\bottomrule
\end{tabular}
\end{table}

FedNova is approximately $2{\times}$ more variable than FedAvg or FedProx
in the symmetric regime ($0.048$ vs $0.019$--$0.024$) and approximately
$3{\times}$ more variable under unequal-$E$ ($0.060$ vs $0.020$--$0.028$).
The drop-stragglers regime shows near-zero instability for FedAvg and
FedProx because the rare-class-holding client is excluded from
aggregation in most rounds, producing a constant aggregation target on
the dominant client's distribution rather than a noisy one. We caveat
the FedNova rows on small $n$ ($5$ and $2$).

\subsection{Empirical adequacy of the Li 2020 B-dissimilarity proxy}
\label{sec:robustness-b-proxy}

\citet{li2020fedprox} introduce a local-dissimilarity bound parameter
$B$ that characterises how much the local gradient at each client
deviates from the global gradient; theoretically, FedProx's gain over
FedAvg should be largest where $B$ is largest. We empirically
proxy $B$ at each ladder partition by the per-round ratio of the
maximum to the mean per-client update norm at FedAvg, averaged over
$150$ rounds and three seeds. Table~\ref{tab:b-proxy} reports the
proxy alongside the FedAvg-vs-FedProx within-pair $\Delta$ at each
partition.

\begin{table}[!htbp]
\centering
\caption{B-dissimilarity proxy (mean ratio of max to mean per-client
update norm at FedAvg) vs FedProx-vs-FedAvg within-pair $\Delta$.}
\label{tab:b-proxy}
\small
\begin{tabular}{lcc}
\toprule
Partition (JS divergence)   & B-proxy & FedProx $\Delta$ macro-F1 \\
\midrule
L0 IID (JS $= 0.000$)        & $1.17$ & $-0.003$ \\
L1 Quantity (JS $= 0.000$)   & $1.44$ & $+0.002$ \\
L2 Label (JS $= 0.104$)      & $1.38$ & $-0.012$ \\
L3 Mixed (JS $= 0.121$)      & $1.51$ & $-0.003$ \\
L4 Severe (JS $= 0.385$)     & $1.61$ & $-0.026$ \\
\bottomrule
\end{tabular}
\end{table}

The empirical B-proxy increases monotonically with heterogeneity (Spearman
$r = +0.90$ vs JS divergence). The FedProx within-pair $\Delta$, however,
shows a weakly \emph{negative} correlation with the B-proxy (Spearman
$r \approx -0.30$ across five partitions): the partitions at which the
theoretical predictor says FedProx should help most are the partitions
where FedProx empirically helps least on this benchmark.

We do not present this as a refutation of the Li 2020 theoretical
analysis. The Wang--Li bound holds under stated smoothness and gradient-
heterogeneity assumptions, and our empirical proxy is a coarse
single-seed approximation. The negative empirical correlation is offered
as a methodological note: on this benchmark, the simple B-proxy is not a
useful predictor of where FedProx delivers its largest gains. The same
caveat applies to other practitioner heuristics that anchor
intervention strength to gradient-norm asymmetry.

% ==============================================================================
% Most-interesting unreported findings:
% A. two collapse modes (instant vs gradual)
% B. early-warning detector at round 20
% C. Yao 2024 directional null result
% ==============================================================================

\section{Two Modes of Rare-Class Collapse}
\label{sec:collapse-modes}

The per-class results of Section~\ref{sec:rare-class-collapse} report
final-round rare-class F1. Inspection of per-round trajectories
reveals that rare-class collapse, when it occurs, follows one of two
qualitatively distinct trajectories.

\subsection{Instant collapse}
Under the drop-stragglers handling of Section~\ref{sec:sh-asymmetric}
and under the perfect-storm condition of
Section~\ref{sec:perfect-storm}, rare-class F1 falls to exactly zero
within the first one or two communication rounds (Li~§5.2 FA+drop:
mean collapse round $= 1.0$, all three seeds; perfect-storm FA+drop:
mean collapse round $= 1.0$, all three seeds). The collapse mode is
\emph{instantaneous}: as soon as Client~1 (the rare-class holder) is
dropped from the aggregation, the global model has no exposure to
rare-class gradients, and the classifier collapses to predicting only
classes present in Client~0's distribution.

\subsection{Gradual collapse}
Under the learning-rate asymmetry of Section~\ref{sec:lr-asymmetry},
rare-class F1 decays over tens of rounds. At D1 5:1 with FedAvg,
mean collapse round is $45.3$ (3/3 seeds collapsed); at D1 5:1 with
FedProx, only 1/3 seeds collapses and only at round $150$ (the final
round). The collapse mode here is \emph{gradual}: rare-class
gradients are still entering the aggregation but are progressively
overwhelmed by the high-learning-rate Client~0 updates that re-bias
the classifier toward the majority class. FedProx slows this process
through proximal regularisation but does not eliminate it.

\subsection{Interpretation}
The two modes have different mechanism signatures and, plausibly,
different mitigations. Instant collapse is a \emph{participation}
problem: the rare-class holder is absent from the aggregation.
Gradual collapse is an \emph{influence-balance} problem: the
rare-class holder is present but its update is dominated. The
practical implication is that the same observed final-round
collapse can arise from causally distinct processes, and a single
intervention is unlikely to address both. See
Figure~\ref{fig:trajectories-d1} and
Figure~\ref{fig:trajectories-li2020}.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.95\linewidth]{F_l4_class_trajectories_d1.pdf}
\caption{Per-class F1 trajectories at D1 5:1 learning-rate asymmetry,
illustrating the gradual collapse mode.}
\label{fig:trajectories-d1}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.95\linewidth]{F_l4_class_trajectories_li2020.pdf}
\caption{Per-class F1 trajectories under the Li 2020 §5.2 protocol,
illustrating the instant collapse mode for FedAvg+drop.}
\label{fig:trajectories-li2020}
\end{figure}

% ------------------------------------------------------------------

\section{Round-20 Early-Warning Detector for Rare-Class Collapse}
\label{sec:early-warning}

The two collapse modes of Section~\ref{sec:collapse-modes} both have
distinguishing signatures within the first few rounds. We tested
whether the per-class F1 distribution at round 20 alone predicts
which runs will eventually collapse.

\subsection{Setup}
For each of 88 L4 training runs pooled across the protocols of this
chapter, we extracted per-class F1 at round 20 plus six aggregate
summary statistics (count of classes below $0.1$, macro-F1, min,
max, range, mean across classes), giving 12 candidate predictors. We
defined collapse as final-round rare-class average F1 $< 0.05$
(positive class). The 88 runs were partitioned into a 76-run
training pool (with 18 positive cases) used for cross-validated
model selection, and an independent 12-run prospective held-out set
(with 1 positive case) used only for final evaluation.

\subsection{Results}
The single strongest predictor is the count of classes whose F1 is
below $0.1$ at round 20 (univariate AUC $= 0.83$ on the training
pool). Macro-F1 at round 20 (AUC $= 0.82$) is nearly as good. A
simple multivariate logistic-regression classifier on the 12
features achieves cross-validated AUC $= 0.84$ on the training pool.
On the prospective held-out set the classifier achieves AUC $= 1.00$,
but this estimate is based on a single positive case and is therefore
preliminary. A conservative operating point at threshold $0.7$
flags $1/12$ held-out runs as at-risk and is correct (precision $= 1$,
recall $= 1$).

\subsection{Interpretation}
Within the protocols tested, rare-class collapse is detectable from
the first 20 communication rounds. The two-mode structure of
Section~\ref{sec:collapse-modes} explains why: instant collapse is
already complete by round 5, and gradual collapse already shows
multiple classes near $F1 = 0$ by round 20. The practical
implication, subject to external validation, is that an early-stopping
or intervention rule based on round-20 features could save the bulk
of the compute spent on runs that will not recover. We present this
as a preliminary observation; with one positive case in the
prospective held-out set, broader validation is required before
clinical deployment.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.85\linewidth]{F_early_warning_roc.pdf}
\caption{ROC curve for the round-20 early-warning classifier on the
training pool (cross-validated AUC $= 0.84$). See
Figure~\ref{fig:early-warning-prospective} for the held-out result.}
\label{fig:early-warning-roc}
\end{figure}

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.85\linewidth]{F_early_warning_prospective.pdf}
\caption{Prospective held-out performance of the round-20 detector.}
\label{fig:early-warning-prospective}
\end{figure}

% ------------------------------------------------------------------

\section{Asymmetric Per-Client $\mu$: Yao 2024 Directional Test}
\label{sec:asymmetric-mu-yao}

\citet{yao2024} predicts that proximal anchoring should benefit most
when applied to the client whose local optimum is furthest from the
global one --- in our 2-client setting, the rare-class-holding
Client~1. We test this directional prediction explicitly.

\subsection{Setup}
Four conditions on engineered L4, three seeds each, 150 rounds:
(i) FedAvg baseline ($\mu_0 = \mu_1 = 0$);
(ii) FedProx symmetric ($\mu_0 = \mu_1 = 0.01$);
(iii) Anchor-large ($\mu_0 = 0.01$, $\mu_1 = 0$): regularise only the
dominant Client~0;
(iv) Anchor-small CTRL ($\mu_0 = 0$, $\mu_1 = 0.01$): regularise only
the minority Client~1, as predicted to help by Yao 2024.

\subsection{Results}
Table~\ref{tab:asymmetric-mu-yao} reports test macro-F1.

\begin{table}[!htbp]
\centering
\caption{Test macro-F1 under asymmetric per-client $\mu$ at L4
(3 seeds; mean $\pm$ sample SD).}
\label{tab:asymmetric-mu-yao}
\small
\begin{tabular}{lc}
\toprule
Condition & macro-F1 \\
\midrule
FedAvg                                                  & $0.469 \pm 0.016$ \\
FedProx symmetric ($\mu_0{=}\mu_1{=}0.01$)              & $0.479 \pm 0.016$ \\
\textbf{Anchor-large} ($\mu_0{=}0.01$, $\mu_1{=}0$)     & $0.495 \pm 0.029$ \\
Anchor-small CTRL ($\mu_0{=}0$, $\mu_1{=}0.01$)         & $0.464 \pm 0.008$ \\
\bottomrule
\end{tabular}
\end{table}

The direction effect is the contrast between Anchor-large and
Anchor-small CTRL: $\Delta_{\text{direction}} = +0.032$ macro-F1, with
a pooled standard deviation across the four conditions of
approximately $0.020$. The effect is approximately $1.6\sigma$:
within the noise envelope on this benchmark. \textbf{The directional
prediction of Yao 2024 is not supported here}: anchoring the dominant
client (Anchor-large) marginally outperforms anchoring the minority
client (Anchor-small) rather than the reverse, but neither asymmetric
configuration meaningfully separates from FedProx symmetric. We do
not claim this refutes Yao 2024 in general --- their analysis
assumes hyperparameter regimes we did not match --- but on this
benchmark the directional prescription provides no reliable
guidance. See Figure~\ref{fig:asymmetric-mu-yao}.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.85\linewidth]{F_asymmetric_mu_L4.pdf}
\caption{Macro-F1 distributions for the four asymmetric-$\mu$
conditions. Box: median, IQR; whiskers: range across seeds.}
\label{fig:asymmetric-mu-yao}
\end{figure}

% ==============================================================================
% Sections added: learning-rate asymmetry (D1), per-class failure mode,
% FedProx x weighted-CE compositionality.
% ==============================================================================

\section{Learning-Rate Asymmetry Between Clients}
\label{sec:lr-asymmetry}

A second form of system-level asymmetry, complementary to the
straggler protocol of Section~\ref{sec:sh-asymmetric}, is
unequal per-client learning rates: an artefact of cross-silo
deployment in which participating sites use different effective
step sizes. We test all three optimisers under this regime on the
engineered L4 partition.

\subsection{Setup}
On L4 we hold $\eta_0 = 0.01$ at Client~0 and vary
$\eta_1 \in \{0.01, 0.005, 0.002\}$ at Client~1, giving learning-rate
ratios $1{:}1$, $2{:}1$, and $5{:}1$. All other hyperparameters
follow Section~\ref{sec:methods-protocol}. The three algorithms
(FedAvg, FedProx with $\mu = 0.01$, FedNova) are each evaluated at
all three ratios with three seeds ($42, 123, 456$) per cell,
yielding 27 runs.

\subsection{Results}
Table~\ref{tab:lr-asymmetry-macro} reports the test macro-F1 at the
endpoints of the ratio range; Figure~\ref{fig:lr-asymmetry} reports
all three ratios.

\begin{table}[!htbp]
\centering
\caption{Test macro-F1 under per-client learning-rate asymmetry on
L4 (3 seeds; mean $\pm$ sample SD, $\textnormal{ddof}=1$).
$\Delta$ is the macro-F1 change from the same algorithm's $1{:}1$
baseline.}
\label{tab:lr-asymmetry-macro}
\small
\begin{tabular}{lccc}
\toprule
Algorithm & $1{:}1$ symmetric & $5{:}1$ asymmetric & $\Delta$ \\
\midrule
FedAvg                 & $0.457 \pm 0.026$ & $0.375 \pm 0.016$ & $-0.083$ \\
FedProx ($\mu = 0.01$) & $0.491 \pm 0.016$ & $0.392 \pm 0.017$ & $-0.099$ \\
\textbf{FedNova}       & $\mathbf{0.509 \pm 0.016}$ & $\mathbf{0.504 \pm 0.007}$ & $\mathbf{-0.006}$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_asymmetric_lr_L4.pdf}
\caption{Macro-F1 and rare-class average F1 under three per-client
learning-rate ratios at L4. Three seeds per cell; error bars are
sample standard deviations across seeds. FedNova is essentially
flat across ratios; FedAvg and FedProx degrade.}
\label{fig:lr-asymmetry}
\end{figure}

Under the $5{:}1$ ratio, FedAvg degrades by $\Delta = -0.083$
macro-F1, FedProx by $\Delta = -0.099$, and FedNova by only
$\Delta = -0.006$, within the observed seed-level variation of its
symmetric baseline. The rare-class average F1 (mean over
dermatofibroma, melanoma, vascular) at $5{:}1$ exhibits the same
ordering at greater magnitude: FedAvg $0.036$, FedProx $0.069$,
FedNova $0.359$. Relative to each algorithm's $1{:}1$ baseline of
$0.230$, $0.315$, $0.359$ respectively, this corresponds to losses
of approximately $84\%$, $78\%$, and no detectable change for
FedNova (whose mean rare-class F1 is essentially identical at the
$1{:}1$ and $5{:}1$ ratios).

Two features warrant comment. First, the algorithm ranking is
\emph{reversed} relative to the straggler-asymmetry result of
Section~\ref{sec:sh-asymmetric}: under learning-rate asymmetry
on this benchmark, FedNova preserves macro-F1 substantially better
than either FedAvg or FedProx. Second, FedProx degrades slightly
more than FedAvg ($\Delta = -0.099$ vs $-0.083$); we report this as
a small effect ($\approx 0.016$ macro-F1, within an order of
magnitude of seed-level noise) that suggests the proximal
regularisation term does not protect against learning-rate
asymmetry in this regime and may slightly worsen the degradation.

\subsection{Mechanism note}
The per-client update-norm logs (Section~\ref{sec:update-norms})
provide a partial explanation. Under the $5{:}1$ ratio, the ratio of
Client~1's mean update norm at $5{:}1$ to its $1{:}1$ baseline is
$0.47$ for FedAvg and $0.50$ for FedProx (approximately proportional
to Client~1's learning-rate reduction), but $0.91$ for FedNova: the
smaller client's effective contribution is approximately preserved
under FedNova while it is attenuated by the learning-rate scaling
under FedAvg and FedProx. We treat this as exploratory evidence
consistent with the macro-F1 ordering rather than as a derivation
from the FedNova convergence analysis of~\citet{wang2020fednova},
which is developed for unequal local-step asymmetry rather than
learning-rate asymmetry.

\subsection{Scope of the claim}
We characterise this result as evidence that \textbf{FedNova is
empirically more robust to learning-rate asymmetry than FedAvg or
FedProx on this benchmark and within the tested asymmetry range}.
The result rests on a single dataset, a single partition (L4), and
a single learning-rate-asymmetry range ($1{:}1$ to $5{:}1$); we do
not extend it to wider asymmetry magnitudes, to other partitions, or
to other datasets.


\section{Per-Class Failure Mode: Rare-Class Collapse Under Asymmetric Protocols}
\label{sec:rare-class-collapse}

The macro-F1 differences in Sections~\ref{sec:sh-asymmetric}
and~\ref{sec:lr-asymmetry} understate the per-class consequences
of optimiser failure on this benchmark. When a federated optimiser
fails under the protocols of this chapter, the failure mode is not
random misclassification but a specific directional pattern:
rare-class inputs are predicted as the dominant majority class
(melanocytic nevi, $1341 / 2005 = 66.9\%$ of the test set).

\subsection{Setup}
We compute row-normalised confusion matrices on the held-out test
set ($n = 2005$) from saved test-time predictions across 91 runs
at L4 spanning eight experimental conditions. For each (algorithm,
condition) pair the confusion matrix is averaged across the
available seeds.

\subsection{Per-class accuracy across L4 protocols}
Figure~\ref{fig:per-class-heatmap} reports the diagonal of the
row-normalised confusion matrix (per-class test accuracy) across
fourteen protocol configurations.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_l4_diagonal_heatmap.pdf}
\caption{Per-class test accuracy across fourteen L4 protocol
configurations. Yellow lines bracket the lower-support and
clinically critical minority classes. Cells with accuracy $< 0.1$
indicate near-total failure on that class. Rows: protocols
ordered from heterogeneity-ladder baseline (top) through D1
$5{:}1$ FedNova (bottom).}
\label{fig:per-class-heatmap}
\end{figure}

Two patterns summarise the failure mode:

\textbf{Misrouting to melanocytic nevi.}\quad The fraction of true
rare-class test inputs (dermatofibroma, melanoma, vascular)
predicted as mel-nevi, averaged across the three rare-class rows of
the row-normalised confusion matrix, ranges from $24\%$ on the
heterogeneity-ladder protocol to $67\%$ under the perfect-storm
FedAvg+drop protocol.\footnote{Computed by
\texttt{analyse\_l4\_confusion\_matrices.py}: for each (algorithm,
protocol) condition we average the row-normalised confusion matrix
across the available seeds, then take the mean of the mel-nevi
column over rows 3, 4 and 6 (dermatofibroma, melanoma, vascular).}
Protocols at which this misrouting exceeds $50\%$ are those at which
the corresponding algorithm's rare-class F1 is at or near zero:
FedAvg+drop under the Li et al.\ asymmetric protocol ($56\%$),
FedAvg+drop under the perfect-storm recipe ($67\%$), FedProx with
$\mu = 1.0$ + $\gamma$-inexact under the perfect-storm recipe
($54\%$), and FedAvg under $5{:}1$ learning-rate asymmetry ($51\%$).
FedProx under $5{:}1$ learning-rate asymmetry sits just below this
threshold ($48\%$).

\textbf{Melanoma detection under learning-rate asymmetry.}\quad At
the $5{:}1$ ratio, the diagonal melanoma accuracy is approximately
$1.2\%$ under FedAvg, $6.1\%$ under FedProx, and $46.3\%$ under
FedNova. Melanoma is the most clinically critical minority class
on this benchmark, and is also the most precisely estimated of the
three rare classes (test support $n = 223$, against $n = 23$ for
dermatofibroma and $n = 29$ for vascular).

\subsection{Confusion patterns at D1 $5{:}1$}
Figure~\ref{fig:confusion-d1-51} reports the row-normalised
confusion matrices for the three algorithms at $1{:}1$ and at
$5{:}1$ learning-rate asymmetry. Under FedAvg and FedProx at $5{:}1$,
approximately half of melanoma test inputs are predicted as
mel-nevi; under FedNova at $5{:}1$, the modal prediction for
melanoma test inputs remains melanoma itself. The qualitative
pattern is similar for vascular lesions; dermatofibroma ($n = 23$)
has insufficient support for confident per-class statements.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{F_l4_confusion_d1_5_1.pdf}
\caption{Row-normalised confusion matrices for FedAvg, FedProx
($\mu = 0.01$), and FedNova at L4, contrasting the $1{:}1$ symmetric
baseline (top row) with the $5{:}1$ learning-rate-asymmetric
condition (bottom row). Yellow rectangles mark the rare-class rows.
Each cell is the per-(true class, predicted class) probability,
averaged across three seeds.}
\label{fig:confusion-d1-51}
\end{figure}

\subsection{Interpretation}
The clinical interpretation is that under unfavourable
algorithm--protocol combinations on this benchmark, a deployed
model does not refuse to predict on rare-class inputs; it
confidently predicts them as the benign majority class. We do not
extend this to a clinical-deployment claim. The DermaMNIST
$28 \times 28$ benchmark is not a screening tool, and the
algorithmic ordering observed here may not transfer to higher-resolution
images, larger client cohorts, or real-world cross-institutional
federations. The observation we make is the narrower one: \textbf{on
this benchmark, the choice of federated optimiser under deployment
asymmetry determines whether the trained model retains useful
sensitivity to the clinically critical minority class}.


\section{Compositionality of FedProx and Class-Weighted Cross-Entropy}
\label{sec:fedprox-wce}

A standard loss-side response to class imbalance is inverse-frequency
class-weighted cross-entropy. We test whether this loss-side
intervention and the FedProx proximal regularisation compose
additively, sub-additively, or super-additively on the engineered
L4 partition under the symmetric protocol.

\subsection{Setup}
A $2 \times 2$ factorial design: $\{\text{FedAvg}, \text{FedProx}~(\mu = 0.01)\}
\times \{\text{standard CE}, \text{inverse-frequency weighted CE}\}$,
three seeds per cell, on the L4 partition.

\subsection{Results}
Table~\ref{tab:wce-2x2} reports the four cell means. Letting $a$
denote (FedAvg, CE), $b$ denote (FedAvg, weighted-CE), $c$ denote
(FedProx, CE), and $d$ denote (FedProx, weighted-CE):
\[
\text{loss\,effect} = b - a = +0.029, \quad
\text{algorithm\,effect} = c - a \approx 0,
\]
\[
\text{combined\,effect} = d - a = +0.027, \quad
\text{interaction} = (d-a) - (b-a) - (c-a) \approx -0.004.
\]

\begin{table}[!htbp]
\centering
\caption{Test macro-F1 in the $2 \times 2$ FedProx $\times$
weighted-CE ablation on L4 (3 seeds; mean $\pm$ sample SD).}
\label{tab:wce-2x2}
\small
\begin{tabular}{lcc}
\toprule
                       & Standard CE     & Weighted CE     \\
\midrule
FedAvg                 & $0.477 \pm 0.002$ & $0.505 \pm 0.023$ \\
FedProx ($\mu = 0.01$) & $0.478 \pm 0.016$ & $0.503 \pm 0.014$ \\
\bottomrule
\end{tabular}
\end{table}

The interaction term is essentially zero ($\approx -0.004$, within
seed-level noise), supporting an additive decomposition of the two
interventions. The algorithm-only effect is itself essentially zero
($\approx 0$, within seed-level variation): \textbf{on this benchmark
and under the symmetric protocol, FedProx adds no meaningful
improvement on top of weighted cross-entropy.} The $+0.029$ benefit
of weighted-CE is preserved under FedProx, but the proximal term
contributes no additional gain.

We do not extend this to a general claim across all loss families.
The result applies to the specific inverse-frequency weighting
tested here on this benchmark, and is offered as evidence that, in
the symmetric regime, loss-side rebalancing is the active
ingredient and that the proximal regularisation does not provide
an additional layer of benefit.


\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
