"""Pure-PyTorch reference FL loop (`server_loop`).

A single-process, sequential implementation with deterministic client
iteration, used for the headline paired-seed runs and as the equivalence
reference for the Flower runtime.
"""
